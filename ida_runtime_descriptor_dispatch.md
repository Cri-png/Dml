# IDA Runtime Descriptor Dispatch

## Current IDB Remap And Refined Descriptor Stride - 2026-06-12

| Address | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| `0x7FF72094FDAE-0x7FF72094FDC4` | Current DML imagebase computes the matched descriptor pointer as `r9 = r13 + s32[r13+0xC0] + (index*3 + 0x18) * 8`. This is equivalent to `r13 + s32[r13+0xC0] + 0xC0 + index*0x18`. | The runtime descriptor table is confirmed as `0x18`-stride in the active IDB, with a `0xC0` bias from the owner-relative table base. | CONFIRMED | Recover the code that populates `owner+0xBC/0xC0` and writes each `0x18` record. |
| `0x7FF72094FDCD` | `movzx ecx, byte ptr [r9+10h]` reads the selector byte from the matched `0x18` descriptor. | The selector remains runtime-local and is not the raw on-disk `0x28` transform-record `+0x10`. | CONFIRMED | Trace stores to descriptor `+0x10`. |
| `0x7FF72094FDD2-0x7FF72094FE7F` | Selector `0` calls builder/interface slot `+0x168`, selector `1` calls `+0x170`, selector `2` calls `+0x178`, and selector `3` calls `+0x180`; the new object/result is returned through stack slot `var_A8`. | The `0..3` selector chooses among four builder/helper methods before the downstream value object is consumed. | CONFIRMED | Resolve the concrete builder interface stored in `var_1F0` to map each selector to a concrete evaluator family. |
| `0x7FF72094EBA8-0x7FF72094EBAC` | At function entry, `r13 = arg0`, `rsi = r13 + 8`, and `var_1F0 = rsi`; the selector block later loads `rax = var_1F0`, `rcx = [rax]`, then calls `[rcx_vtable + 0x168/0x170/0x178/0x180]`. | The selector builder interface is the object pointer stored at `arg0 + 8`. This is a concrete owner relationship for the four selector routes. | CONFIRMED | Trace the caller that creates `arg0` and initializes its `+8` helper object. |
| `0x7FF72094E940` | Wrapper calls `(*(*[a1+8]).vtable + 0x130)` to create a node-like object, then calls `sub_7FF72094EB20(a1, ..., a3, a4, ...)`, then calls the created node's `+0xC8` method and `Scene_FinalizeLoadedNode`. | The same `arg0+8` helper object that owns the selector-builder routes is also used to create the loaded node object. | CONFIRMED | Resolve the concrete helper class behind `a1+8`. |
| `0x7FF720950BB0` | This caller is reached from `Scene_LoadNodeAssetAsync_Callback`, loops child entries with `v14 += 0x60`, and calls `sub_7FF72094EB20(...)` for each child/entry before attaching the result through the receiver's `+0xC8` method. | `sub_7FF72094EB20` is on the loaded-node and child-scene assembly path, not an isolated math-only animation routine. | CONFIRMED | Connect this load-time path to the runtime animation object that later supplies advancing time. |
| `0x7FF72095005B-0x7FF7209500E3` | A nearby inline construction path on receiver `rdi` applies local translation through `+0x168`, local rotation through `+0x158`, and local scale through `+0x148`. | This confirms the local TRS sink slot ordering in the active imagebase: scale `+0x148`, rotation `+0x158`, translation `+0x168`. | CONFIRMED | Prove whether the selector-built value object ultimately writes through this same receiver interface during playback. |

## Proven Runtime Descriptor Origin for `r9`

| Address | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| `0x7FF7A7ECFCCE-0x7FF7A7ECFD08` | Case-12 path loads `r13 = [ [ [old_r13] + 0x40 ] + 0x28 ]`, then reads `count = *(int *)(r13 + 0xBC)` and `rel = *(int *)(r13 + 0xC0)`. It sets `base = r13 + 0xC0 + rel`, then loops `strcmp([base + index*0x18], [r12+1])`. | The selector path is operating on a runtime array of named descriptors owned by a runtime object, not on raw BRES bytes. The descriptor stride is `0x18` bytes and the first qword is a name pointer/string. | CONFIRMED | Recover more field uses within the `0x18`-byte descriptor to identify its full layout. |
| `0x7FF7A7ECFD00` | `lea rdx, [r12+1] ; Str2`, `mov rcx, [rbx] ; Str1`, `call strcmp`. | The runtime descriptor name at `[rbx]` is matched against a name string taken from another object/record chain rooted in `r12`. | CONFIRMED | Prove whether the `r12` name source is the file-side transform track record or a related binding record. |
| `0x7FF7A7ECFDAE-0x7FF7A7ECFDC4` | On match: `r9 = r13 + rel + 0xC0 + index*0x18`. | `r9` is the matched runtime descriptor record. | CONFIRMED | Use this derivation whenever reasoning about `[r9+...]` fields. |

## The Selector Byte

| Address | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| `0x7FF7A7ECFDCD` | `movzx ecx, byte ptr [r9+10h]` | The selector is a single byte field at offset `+0x10` inside the matched runtime descriptor record. | CONFIRMED | Continue identifying what the other descriptor bytes/qwords mean. |
| `0x7FF7A7ECFDD2-0x7FF7A7ECFDE7` | Branch ladder: selector `0` -> `loc_FE68`; selector `1` -> `loc_FE3F`; selector `2` -> `loc_FE16`; selector `3` -> `loc_FDED`; any other value falls back to failure path. | At least four selector values are implemented locally. | CONFIRMED | Search other functions for writes into descriptor `+0x10` if they exist. |

## Selector Value Mapping

| Selector value | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| `0` | `loc_FE68` calls virtual slot `[handler_vtable + 0x168]`; the table entry at `0x7FF7AA2B34C8` is `sub_7FF7A7ED86F0`, which tail-jumps into `sub_7FF7A7EDCC30`. | Selector `0` routes to the “single packed quaternion decode, no explicit consumer wrapper” path. | CONFIRMED | Determine later whether the returned object is consumed elsewhere. |
| `1` | `loc_FE3F` calls `[handler_vtable + 0x170]`; the table entry at `0x7FF7AA2B34D0` is `sub_7FF7A7ED86C0`, which tail-jumps into `sub_7FF7A7EDDA10`. | Selector `1` routes to the `EDDA10` quaternion path without the explicit `+0x158` consumer wrapper seen in `ECD090`. | CONFIRMED | Determine whether this variant returns an intermediate object/value instead of applying immediately. |
| `2` | `loc_FE16` calls `[handler_vtable + 0x178]`; the table entry at `0x7FF7AA2B34D8` is `sub_7FF7A7ECD110`, which calls `EDCC30` then forwards the decoded buffer to `+0x158`. | Selector `2` routes to the single-quaternion consume-wrapper path. | CONFIRMED | Trace what kind of receiver object gets passed to `+0x158`. |
| `3` | `loc_FDED` calls `[handler_vtable + 0x180]`; the table entry at `0x7FF7AA2B34E0` is `sub_7FF7A7ECD090`, which calls `EDDA10` then forwards the decoded buffer to `+0x158`. | Selector `3` routes to the paired/interpolated consume-wrapper path. | CONFIRMED | Same next step as selector `2`. |

## Best-Supported Meaning of the Selector

| Address | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| selector values `0/2` vs `1/3` | `0` and `2` both use `EDCC30`; `1` and `3` both use `EDDA10`/`ED17B0`. | The low bit most likely distinguishes decode family: single packed quaternion vs paired/interpolated packed quaternion. | STRONGLY SUPPORTED | Inspect descriptor creation/population code for explicit writes of selector values. |
| selector values `0/1` vs `2/3` | `2` and `3` use consume wrappers that call the receiver’s `+0x158`; `0` and `1` do not. | The next bit most likely distinguishes “decode only / return intermediate” vs “decode and immediately forward to consumer”. | STRONGLY SUPPORTED | Prove by locating where the non-consuming return values are used. |
| `[r9+0x10]` overall | Field is inside a matched named runtime descriptor, not the raw on-disk record. | It most likely represents a runtime descriptor type/behavior selector for packed rotation handling. | CONFIRMED | Tie the runtime descriptor back to its file-side source record more directly. |

## Current IDB Selector Builder And Descriptor Field Map - 2026-06-13

| Address | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| `0x7FF722D333A0` | Static object begins with vptr `off_7FF722D33068`; it is initialized by tiny wrapper `0x7FF722717B50 -> sub_7FF7209AF990(&off_7FF722D333A0)`, and `sub_7FF7209AF990` writes `off_7FF722D33068` into the object. | This is a concrete singleton/helper object for the node/animation construction route, not an arbitrary table. | CONFIRMED | Find which higher-level scene/resource object stores or requests this singleton. |
| `off_7FF722D33068 + 0x130` | Slot `+0x130` is `0x7FF7209B26F0`; this is the same node factory slot used by `sub_7FF72094E940`. It allocates a `0x2A0` node-like object and calls constructor `0x7FF7209A5A70`. | The helper object supplies both loaded-node construction and descriptor selector builders. | CONFIRMED | Continue resolving the created node class and its transform sink implementations. |
| `off_7FF722D33068 + 0x168/+0x170/+0x178/+0x180` | Slots are `0x7FF7209B2540`, `0x7FF7209B2660`, `0x7FF7209B24B0`, and `0x7FF7209B25D0`. These are exactly the four virtual calls reached from selector values `0..3`. | The runtime selector maps to four concrete builder methods on this helper singleton. | CONFIRMED | Tie one live populated descriptor to one builder output object. |
| `0x7FF72094FDAE-0x7FF72094FE7F` | The selector dispatch leaves the matched descriptor pointer in `r9`; the virtual builder call sets only `rcx`, `rdx`, and `r8`, so `r9` is passed as the fourth argument. | The selector-created object is built with the same `0x18` descriptor whose byte `+0x10` chose the builder. | CONFIRMED | Confirm the calling convention against the constructor inputs for a second path. |
| `0x7FF7209B2540` | Selector `0` builder zeroes output, allocates `0x200`, clears it, then calls `0x7FF720A00F40(obj, context, descriptor, 1)`. | Selector `0` creates a `0x200` runtime evaluator/descriptor object. | CONFIRMED | Identify which descriptor value family selects `0` in a live table. |
| `0x7FF7209B2660` | Selector `1` builder allocates `0x208`, clears it, then calls `0x7FF720A016A0`. | Selector `1` creates a slightly larger runtime evaluator/descriptor object. | CONFIRMED | Identify whether this corresponds to an interpolated or richer sample family. |
| `0x7FF7209B24B0` | Selector `2` builder allocates `0x210`, clears it, then calls `0x7FF720A00740`. | Selector `2` creates a `0x210` runtime evaluator/descriptor object. | CONFIRMED | Compare constructor field count against selector `0/1/3`. |
| `0x7FF7209B25D0` | Selector `3` builder allocates `0x228`, clears it, then calls `0x7FF7209FFCF0`. | Selector `3` creates the largest of the four runtime evaluator/descriptor objects. | CONFIRMED | Determine whether the extra copied fields explain the special packed/interpolated variants. |
| common constructor `0x7FF720A368D0` | Stores the descriptor pointer at object `+424`, copies `byte [descriptor+0x11] != 0` into object `+432`, copies `qword [descriptor+0x00]` into object `+376`, and calls `0x7FF72092E9E0(obj, qword [descriptor+0x08])`. | Runtime descriptor fields are now partly proven: `+0x00` and `+0x08` are object/name/value handles used by the common base, `+0x10` is selector, `+0x11` is a flag, and `+0x14` is used later as a relative parameter pointer. | CONFIRMED | Determine the exact meaning of `+0x00`, `+0x08`, and `+0x11` by following their consumers. |
| `0x7FF72092E9E0` | Treats its second argument as `char *`, computes `strlen(a2)`, and copies it into object storage at `a1 + 56` through `0x7FF720921240`. The common selector constructor calls it with `qword [descriptor+0x08]`. | Descriptor `+0x08` is a string pointer consumed as the selector object's local name/binding text. | CONFIRMED | Compare the copied string against known track/node names in a live or dumped descriptor table. |
| selector primary vtable `+0x210` -> `0x7FF720A00360` | After selector object creation, dispatcher calls primary vtable slot `+0x210` with `edx = byte [object+0x1B0]` in the surrounding path. The shared method updates object `+440`: if object `+280` is null it chooses `object+152` when object `+432` is true, otherwise `object+88`; if object `+280` exists it either calls `sub_7FF72092D190()` for the true flag or virtual `+136` on object `+280`. | Descriptor flag `+0x11` copied to object `+432` controls which internal target block is active, and vtable `+0x210` refreshes that active-target pointer. | CONFIRMED for control flow, NOT YET KNOWN for semantic label | Determine what object `+88`, `+152`, `+280`, and `+440` represent in evaluator output. |
| selector constructors `0xA00F40`, `0xA016A0`, `0xA00740`, `0x9FFCF0` | Each constructor reads `payload = descriptor+0x14 + s32[descriptor+0x14]`, then copies a selector-specific number of fields from that payload into object offsets around `+448`. Selector `0` copies three dwords into `+448/+452/+456`; selector `1` copies six dwords into `+448/+452/+456/+460/+464/+468`; selector `2` copies seven dwords into `+448..+472`; selector `3` copies twelve dwords plus flag bytes into `+448..+501`. | Descriptor `+0x14` is a relative pointer to selector-specific parameter payload. The selector byte does not just branch to math; it chooses a parameterized runtime object class. | CONFIRMED | Match these copied dword fields to file-side row/stride/base descriptors for ordinary shared tracks. |
| selector object vtables | Selector constructors install primary vtables `off_7FF722D3EF78`, `off_7FF722D3F1E0`, `off_7FF722D3ED10`, and `off_7FF722D3EAA8`, plus secondary interface vtables at object `+8` and related offsets. The primary vtables are mostly identical except constructor/destructor-tail slots. | The four selector products are related runtime object classes; their behavioral difference is likely driven by copied descriptor parameters and secondary/interface usage, not a wholly different large primary interface. | STRONGLY SUPPORTED | Inspect secondary interface calls from `0x9F9CF0` and later sinks to map the exact evaluator slot family per selector. |

## Current Runtime Descriptor Layout Draft

| Offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| `+0x00` | Copied by common constructor into runtime object `+376`; earlier lookup also treats descriptor `[0]` as a name/string pointer during `strcmp`. | Name or binding identifier pointer/handle. | CONFIRMED as consumed, STRONGLY SUPPORTED as name/binding | Follow all reads of object `+376`. |
| `+0x08` | Passed to `0x7FF72092E9E0(obj, qword [descriptor+0x08])`; `0x92E9E0` treats it as `char *`, calls `strlen`, and copies it into object storage at `+56`. | Runtime string pointer, probably local name/binding/property text for the selector object. | CONFIRMED as a string, STRONGLY SUPPORTED as binding/name | Compare against known track names in a live descriptor table. |
| `+0x10` | Read by `0x7FF72094FDCD` and branches `0..3`. | Runtime selector byte choosing one of four builder/object families. | CONFIRMED | Find table population and exact assigned value for one named track. |
| `+0x11` | Common constructor stores `byte [descriptor+0x11] != 0` at object `+432`; vtable `+0x210` method `0xA00360` uses it to choose/update object `+440` between internal target blocks or an external object path. | Runtime flag selecting active target/source block for the built evaluator object. | CONFIRMED as a control flag, NOT YET KNOWN semantically | Compare objects with `+0x11 = 0/1` if a populated table is dumped. |
| `+0x14` | Selector-specific constructors read `s32[descriptor+0x14]` and add it to `descriptor+0x14` to reach parameter payload. | Relative pointer to selector-specific parameter payload. | CONFIRMED | Tie payload dwords to file-side compact descriptors such as row, stride, base offset. |

## Runtime Field-Offset Table And Broad Object Cooker - 2026-06-13 Follow-Up

| Address | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| `0x7FF720A110A0` | Builds `qword_7FF722E9DE20` only if the global vector is empty. It loops `i = 0..118`, calls a virtual field-query method on a freshly allocated runtime object, and appends either `field_ptr - object_base` or `-1` into the global vector. | `qword_7FF722E9DE20` is a dynamic runtime field-offset table with 119 entries. It is not itself the animation descriptor table. | CONFIRMED | Use this table distinction whenever reading code that references `qword_7FF722E9DE20 + index`. |
| `0x7FF720A10EC0` | Allocates a `0x528` runtime object, constructs it through `0x7FF7209BD8B0`, links it into a list, calls `0x7FF720A110A0`, and stores a back-link at object `+1008`. | This is a runtime object allocator/factory used by the broader BRES object construction path. | CONFIRMED | Determine which parsed BRES owner types map to this `0x528` runtime object family. |
| `0x7FF7209BD8B0` | Initializes multiple internal subobjects at offsets including `+120`, `+200`, `+312`, `+336`, `+432`, `+584`, `+720`, and `+968`, installs vtables, zeroes fields near `+1008..+1032`, and initializes a lock/list at `+1040`. | The allocated object is a large polymorphic runtime scene/resource object, not a compact animation descriptor entry. | CONFIRMED | Identify which subobject interface exposes the 119 field queries used by `0x7FF720A110A0`. |
| `0x7FF7209B7920` entry | Receives an owning construction object, stores a ref-counted pointer at owner `+0x200`, reads a source object from owner `+0x1B0`, calls `0x7FF7209B6620`, then creates a runtime object through `0x7FF720A10EC0` and stores it at owner `+0x1A8`. | This function is a strong broad BRES/source-record to runtime-object cooker/translator candidate. | STRONGLY SUPPORTED | Tie its source object at owner `+0x1B0` back to a concrete BRES file record in one sample. |
| `0x7FF7209B7A70-0x7FF7209B7C4D` | Copies many source fields into runtime object fields via `qword_7FF722E9DE20`: source bytes `+0x10/+0x11/+0x12/+0x13`, source dword assembled from bytes `+0x14..+0x17`, and source vec4 blocks at `+0x18/+0x28`. | This is field-by-field translation from a source record into dynamic runtime object fields. | CONFIRMED for copying, STRONGLY SUPPORTED for source-record translation | Match those source offsets against a BRES record family in local runtime files. |
| `0x7FF7209B7CE0-0x7FF7209B7EFF` | Switches on source dword `+0x40` with 7 cases. Cases map a relative payload at source `+0x44` into one, two, or three float runtime fields via dynamic field offsets `+0xAC/+0xB0/+0xB4`. | This is a value-format switch for source-record payloads, separate from the cooked `0x18` descriptor selector at descriptor `+0x10`. | CONFIRMED for switch mechanics, NOT YET KNOWN semantically | Determine whether source `+0x40` means scalar/vec2/vec3/component swizzle or another BRES value class. |
| `0x7FF7209B86DE-0x7FF7209B88CE` | Branches on source byte `+0x68`; when `0` or `2`, follows a relative subrecord at source `+0x6C` and copies small fields plus vec3-like blocks into runtime dynamic fields. | Additional source-record substructure is cooked into the same runtime object family. | CONFIRMED for control/data flow, NOT YET KNOWN semantically | Compare this source subrecord shape against known material/profile/texture records. |
| `0x7FF7209B8B3D-0x7FF7209B9131` | Copies source string at `+0x180` through `sub_7FF7209B5910` using type id `0x29`, then processes source string at `+0x1D8`; non-default strings route through helper lookups and are stored via `sub_7FF7209B5910` with type id `0x66`. | The broad object cooker also resolves/copies named resource or binding strings into dynamic runtime fields. | CONFIRMED | Compare copied strings to known BDAE names, material names, or node names in a runtime file. |
| `0x7FF7209B5910` | Looks up `qword_7FF722E9DE20[typeId]`; if valid, copies an SSO string into `object + fieldOffset`; then clears/frees the source string object. | This is a dynamic-field string setter, not a descriptor-table writer. | CONFIRMED | Use calls to this helper to map runtime field IDs to source string roles. |
| `0x7FF7209B59B0` | Concatenates two SSO strings into a destination SSO string. Called by `0x7FF7209B7920` before a `sub_7FF7209B5910(..., 0x66, ...)` call. | Part of resource/binding name construction inside the broad object cooker. | CONFIRMED | Inspect the two input strings for a known BRES record to identify the constructed name form. |

## Descriptor Table Builder Status After Follow-Up

| Address | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| `0x7FF72094FCDF-0x7FF72094FDC4` | Case 12 obtains `r13 = [[[old_r13] + 0x40] + 0x28]`, reads `count = s32[r13+0xBC]`, table relative offset `rel = s32[r13+0xC0]`, and searches `r13 + rel + 0xC0` as a `0x18`-stride named descriptor table. | The selector table is read as a BRES-style relative subtable. It may be relocated/cooked file data rather than a heap vector populated by obvious stores. | STRONGLY SUPPORTED | Search local BRES bytes for matching compact `0x18` tables near owner-like records, and continue tracing the owner family. |
| `0x7FF7209F21F0` | Separately confirmed as a generic `0x18` vector insertion helper: it inserts/copies a 24-byte entry with fields at `+0`, `+8`, and `+0x10`. | This is a real `0x18` table helper, but it is not yet proven to build the case-12 descriptor table. | CONFIRMED as a 0x18 vector helper, NOT YET KNOWN as the case-12 builder | Continue from its caller `0x7FF7209F29C0` only if evidence ties that side table to case-12 descriptors. |
| `0x7FF7209B7920` | Broadly cooks runtime scene/resource objects and uses dynamic field offsets, but current evidence did not show stores into the case-12 owner `+0xBC/+0xC0` table or descriptor byte `+0x10`. | This is an upstream construction lead, not the exact cooked descriptor-table builder yet. | STRONGLY SUPPORTED lead, NOT YET KNOWN for exact descriptor-table population | Follow the object created at owner `+0x1A8` and its subobjects to see whether one owns the case-12 descriptor table. |
## Related Construction Context

| Address | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| `0x7FF7A7ECEBA1-0x7FF7A7ECECB6` | `r15` input object is queried at `+0x58` and `+0x4C`; runtime-side helper object is taken from `a1 + 8`; a method at `[handler_vtable + 0xE0]` is also consulted before the main 16-case switch. | The selector dispatch lives inside a larger object-construction/translation routine, not an isolated math kernel. | STRONGLY SUPPORTED | Continue mapping the 16 case codes from the main `jpt_7FF7A7ECEDC1`. |
| `0x7FF7A7ECEDA4-0x7FF7A7ECEDC1` | Main switch selects on `*(int *)(r15 + 0x54 + base + index*8 + 4) - 1`, giving 16 cases total. The selector-byte path sits specifically in case `12`. | The selector-byte runtime descriptor dispatch is a subcase inside a broader file/object translation pipeline. | CONFIRMED | Determine what file/object type “case 12” actually represents. |
