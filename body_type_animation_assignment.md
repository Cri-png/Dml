# Body Type Animation Assignment

## Current Best-Supported Decision Chain

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| `data.bud -> dragon_types.json` | Per-dragon entries include `ID`, `UID`, `MESHES`, `EGG_MESH`, and `BODY_TYPE`. | The game defines each dragon's shared body-family membership in shipped data, not by inferring it from the mesh filename alone. | CONFIRMED | Trace the executable JSON consumer for `dragon_types.json` in IDA if we want code-side proof of load order. |
| `data.bud -> dragon_body_type_specific_data.json` | Entries keyed by `BODY_TYPE` define `ANIMATIONS`, `HABITAT_MOVE_DIST_RANGE`, `HABITAT_IDLE_TIME_MS_RANGE`, and `HABITAT_NUM_ANIM_RANGE`. | Shared animation bundles are selected from body-type data, not from the dragon runtime mesh BDAE itself. | CONFIRMED | Search IDA for the consumer of this JSON table and for the point where `BODY_TYPE` turns into a loaded animation asset name. |
| current DML IDB `0x7FF720CF3EE0` | Parser code reads `HABITAT_MOVE_DIST_RANGE`, `HABITAT_IDLE_TIME_MS_RANGE`, `HABITAT_NUM_ANIM_RANGE`, `ANIMATIONS`, and `BODY_TYPE`; each `ANIMATIONS` string is copied into a `0x20`-stride array and the literal `.bdae` is appended. | The executable consumes the body-type JSON as the source of exact shared-animation BDAE asset names. | CONFIRMED | Trace the caller `0x7FF720AED7C0` forward to the resource load call if we need load-order proof. |
| `data.bud -> dragons_portraits_animations.json` | Portrait animation lists are also keyed by `BODY_TYPE` and `BODY_TYPE_BABY`. | The same body-type key drives at least one other animation-selection subsystem beyond habitat/shared motion. | CONFIRMED | Compare portrait/body animation selection call sites if we later need UI portrait playback. |
| `data.bud -> localization_en.json` plus `localization_override_en.json` in `generated/cache/bud_asset_index.json` | The base archive index already proves shipped localization tables exist alongside dragon metadata, and the override file is much smaller than the base table. | The future viewer should resolve dragon display names through localization tables layered on top of authoritative dragon IDs, not through mesh filenames. | STRONGLY SUPPORTED | Extract the English localization tables and identify the exact key shape for one known dragon such as `D_TIKI_2024`. |

## Tiki 2024 Resolution

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| `dragon_types.json` entry `ID = D_TIKI_2024` | `UID = 1105`, `BODY_TYPE = GORILLA`, `MESHES = ["dragon_tiki_2024_baby", "dragon_tiki_2024"]`, `EGG_MESH = "egg_tiki_2024"`, `RARITY = "PRIMAL"`. | Tiki 2024 is explicitly tagged by shipped game data as `BODY_TYPE = GORILLA`. | CONFIRMED | If needed, find the localized name table that maps `D_TIKI_2024` to the display name "Masked Dragon". |
| `dragon_body_type_specific_data.json` entry `BODY_TYPE = GORILLA` | `ANIMATIONS = ["dragon_gorillabody_baby_anim", "dragon_gorillabody_anim", "dragon_gorillabody_anim"]`. | Adult Tiki 2024 should inherit `dragon_gorillabody_anim`; baby Tiki 2024 should inherit `dragon_gorillabody_baby_anim`. | CONFIRMED | Verify the same asset names on the executable-side resource-load path if we want code proof beyond data proof. |
| `game_dlc_dragon_anims_w8_default_1.pak` | Exact archive hits exist for `dragon_gorillabody_anim.bdae` and `dragon_gorillabody_baby_anim.bdae`. | The Tiki 2024 shared animation family lives in the dedicated dragon-animation DLC pak, matching the resolved `GORILLA` body type. | CONFIRMED | Extract both anim BDAEs into a regression bundle for future viewer playback work. |
| `dragon_tiki_2024.bdae` vs `dragon_gorillabody_anim.bdae` | The Tiki adult mesh skeleton has `99` named `-node` entries. The gorilla shared anim file has `55` transform-track base names, and all `55` are present in Tiki's mesh skeleton. `missingFromMeshSample = []`. | The data-driven `GORILLA` assignment is also consistent with the actual Tiki skeleton/track naming. | CONFIRMED | Compare against another `BODY_TYPE = GORILLA` dragon mesh to see whether the same shared anim file covers multiple gorilla-family rigs equally cleanly. |

## Important Correction

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| `dragon_gorilla.bdae` vs `dragon_gorillabody_anim.bdae` | `dragon_gorilla.bdae` renders as a different body mesh silhouette, while `dragon_gorillabody_anim.bdae` matches Tiki's skeleton track names. | Shared animation family and body mesh asset are separate concerns. `gorillabody_anim` is the correct shared anim family for Tiki 2024, but `dragon_gorilla.bdae` is not the correct Tiki mesh. | CONFIRMED | Keep viewer logic separate: mesh selection from the dragon's own `MESHES` entry, shared animations from the resolved `BODY_TYPE` mapping. |

## Viewer-Ready Rule

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| future viewer lookup chain | `dragon_types.json` can be queried by dragon `ID` or mesh stem, then `BODY_TYPE` can be joined into `dragon_body_type_specific_data.json`, then exact archive search can locate the shared anim BDAEs, and finally `localization_<lang>.json` plus `localization_override_<lang>.json` can resolve the displayed species name. | The future viewer should search by authoritative dragon entry metadata first, then add localization as a separate display-name layer instead of inferring names from asset filenames. | STRONGLY SUPPORTED | Extend the resolver flow with extracted localization tables after the exact key shape is confirmed. |
| current DML IDB `0x7FF721552F60 -> 0x7FF7215515C0 -> 0x7FF720952D00` | Runtime playback switches by a requested clip string, tries `custom_` plus that clip name first, then falls back to the plain clip name in the shared animation BDAE's `0x18` clip table. | Viewer UI should show shared body-type clips by their real names and support future `custom_*` override discovery rather than guessing one global animation. | CONFIRMED | Add a clip-browser layer that lists table entries and flags whether a custom override exists. |

## Tiki Shared Animation Application Probe

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| `generated/experimental/dragon_tiki_2024_runtime/shared_anim_probe/probe_shared_animation_application.json` | The probe resolves `dragon_tiki_2024.bdae` to `D_TIKI_2024`, `BODY_TYPE = GORILLA`, chooses `dragon_gorillabody_anim.bdae`, and binds all `55/55` shared anim track node bases to Tiki mesh nodes with `missingFromMeshCount = 0`. | The authoritative shared-anim assignment is not only data-correct, but directly applicable to the Tiki adult mesh skeleton by exact node-name binding. | CONFIRMED | Extend the probe into a pose evaluator that outputs local TRS per frame for a larger set of shared anim tracks. |
| `generated/experimental/dragon_tiki_2024_runtime/shared_anim_probe/probe_shared_animation_application.json -> tailStride4Patch` | The experimental Tiki tail patch is enabled for adult `dragon_gorillabody_anim.bdae`, using packet base `0xBEF48` and lane map `{21:3, 22:1, 23:2}` for `Tail_bone01/02/03-node`. | The inherited gorilla anim contains at least one special packed rotation payload that can already be decoded into per-frame local quaternions for Tiki tail bones. | STRONGLY SUPPORTED | Compare the same decoded tail frames against executable-side setter inputs when IDA access is available again in tool form. |
| `generated/experimental/dragon_tiki_2024_runtime/shared_anim_probe/probe_shared_animation_application.json -> validationAnchors` | Best angular errors against known runtime anchors are approximately `0.0433°`, `0.0624°`, `0.0473°`, and `0.1454°`. | The stride-4 tail decoder is not just structurally plausible; it is numerically aligned with previously captured runtime data to within a small error band. | STRONGLY SUPPORTED | Validate a second shared anim segment or a second dragon in the same `BODY_TYPE = GORILLA` family. |
| `generated/experimental/dragon_tiki_2024_runtime/shared_anim_probe/dragon_gorillabody_anim_probe.md` | File-side probe of `dragon_gorillabody_anim.bdae` finds `66` total tracks (`51` rotation, `12` translation, `3` scale), with only `4` tracks carrying non-zero `extraRel`: `IKRFChain-node-rotation`, `shadow_node-node-scale`, `shadow_node-node-translation`, `speed_node-node-translation`. | The inherited gorilla anim is a full shared transform-track bundle, but the Tiki tail special-case is not signaled by `extraRel != 0`; it remains a separate packed/runtime-decoder problem. | CONFIRMED | Tie the tail slots to the cooked runtime descriptor path rather than relying on `extraRel` heuristics. |
| current DML IDB remap: `0x7FF720951CA0 -> 0x7FF72095DAF0` | The engine-side helper decodes two quaternions from `16-byte` sample records carrying four explicit `24-bit` quantized component fields, then blends them through the shared quaternion blender `0x7FF720955D60`. | The experimentally decoded Tiki tail stride-4 packets are now strongly supported as belonging to a real engine-side packed-quaternion family rather than an isolated ad hoc patch. | STRONGLY SUPPORTED | Prove that a cooked runtime descriptor for one of `Tail_bone01/02/03-node-rotation` reaches this helper family. |
| `generated/experimental/dragon_tiki_2024_runtime/shared_anim_pose/shared_anim_debug_pose.json` | The new debug pose export resolves the same `BODY_TYPE = GORILLA` assignment and emits `48` frames of local node transforms for exactly `3` decoded nodes: `Tail_bone01-node`, `Tail_bone02-node`, and `Tail_bone03-node`. | The project now has a viewer-ready intermediate export format for the currently proven animation subset: authoritative assignment + exact node binding + per-frame local quaternions for the known tail patch. | CONFIRMED | Feed this debug pose subset into a renderer-side pose overlay before attempting generalized shared-body playback. |
| `generated/experimental/dragon_tiki_2024_runtime/shared_anim_probe/dragon_gorillabody_anim_descriptors.json` | Corrected descriptor inventory shows each normal `0x28` track record uses `inputRel -> 0x18 input/time descriptor` and `outputRel -> 0x18 output/value-row descriptor`. All `66` tracks use time mode `3`; output families are `50` ordinary rotations (`valueKind=4`, `stride=16`), `10` ordinary translations (`valueKind=3`, `stride=12`), `2` ordinary scales (`valueKind=3`, `stride=12`), and `4` scalar/special tracks (`valueKind=1`, `stride=4`). | This supersedes the older qword-only `outputRel + 0x24` sentinel interpretation. `outputRel` is a real compact descriptor, not dead output data. | CONFIRMED | Keep using the corrected input/output descriptor model for playback tooling. |
| `dragon_gorillabody_anim.bdae -> head_ctrl-node-rotation` | The ordinary rotation track record at `recordOffset = 0x4DC0` has `inputRel = 0xD14`, and the qword at `recordOffset + inputRel + 0x24 = 0x5AF8` is `0x0010000000000041`. Parsed little-endian fields: `stride = 0x10`, `baseOffset = 0`, `rowIndex = 0x41`. | We now have one exact on-disk ordinary-rotation descriptor anchor, not just a family summary. | CONFIRMED | Match this `stride = 16`, `row = 0x41` descriptor to one concrete runtime rotation-handler family. |
| `dragon_gorillabody_anim.bdae -> body_root-node-translation` | The ordinary translation track record at `recordOffset = 0x49B0` has `inputRel = 0xB34`, and the qword at `recordOffset + inputRel + 0x24 = 0x5508` is `0x000C00000000000D`. Parsed fields: `stride = 0x0C`, `baseOffset = 0`, `rowIndex = 0x0D`. | We now have one exact on-disk ordinary-translation descriptor anchor alongside the rotation anchor. | CONFIRMED | Match this `stride = 12`, `row = 0x0D` descriptor to the runtime translation-family sampler/decoder path. |
| `dragon_gorillabody_anim.bdae` row table candidate | `inspect_shared_anim_descriptors.py --find-row-table` finds row table base `0x6604`, validates all `66/66` tracks, and uses row entry formula `sample = rowTableBase + rowIndex * 8 + s32(row + 0)`. Examples: `body_root-node-translation` time row `0x0C` decodes `[0,1,2,3,4,5]` and value row `0x0D` decodes vec3 translations; `head_ctrl-node-rotation` time row `0x40` decodes `[0,60,100,106,107,108]` and value row `0x41` decodes normalized XYZW quaternions. | The ordinary shared-body animation payload is now file-side decodable without the old raw-`inputTarget` shortcut. | STRONGLY SUPPORTED | Confirm the same row-table formula against executable sampler pseudocode once MCP budget is available again. |
| `generated/experimental/dragon_tiki_2024_runtime/shared_anim_clip_table/dragon_gorillabody_anim_clip_table.md` | Clip-table inspector finds `33` named `0x18` entries in `dragon_gorillabody_anim.bdae`, including `idle_basic = 0..2000`, `idle_random_1 = 3333..6167`, `idle_walk = 6667..8333`, `levelup = 40000..41833`, `attack = 60000..61333`, and `win = 80000..81967`. | After resolving Tiki to `BODY_TYPE = GORILLA`, the viewer should expose named animation states from the shared BDAE instead of showing the BDAE as a single undifferentiated animation. | CONFIRMED | Prove the final clip-time-to-row-time mapping. |
| `generated/experimental/dragon_tiki_2024_runtime/shared_anim_pose/dragon_gorillabody_anim_keyframes.json` | New keyframe export decodes `62` local transform tracks (`50` rotation, `10` translation, `2` scale), keeps `4` scalar/special tracks unresolved, and emits `51` preview frames at frame step `5` over max raw frame `250`. | Tiki shared-body playback is now past assignment/binding and into descriptor-row local TRS decoding. It is not skinned mesh playback yet, but the main ordinary channel data is no longer blocked. | STRONGLY SUPPORTED | Feed these local TRS frames into the skeleton/skin preview path, with the special `valueKind=1` tracks still disabled/debug-colored. |
| current DML IDB `0x7FF7209B49B0` / `0x7FF7209B51D0` | The mode-3 time helper reads `u16` key times and multiplies them by `33.333332` before interpolation. All `dragon_gorillabody_anim.bdae` tracks currently use this time mode. | The viewer should treat raw time rows as engine-time keys, not literal display frames. | CONFIRMED | Find the clip/subclip metadata that selects the live time window for each animation state. |
| current DML IDB `0x7FF7209F9CF0` | This evaluator takes a live `float` time argument, forwards it into `0x9B51D0`, receives key/alpha state, and then calls downstream virtual evaluators. | Descriptor-row keyframes are on the runtime animation evaluation/application path, not only a load-time file parser path. | STRONGLY SUPPORTED | Resolve the virtual methods called after sampling to prove the final property sinks. |
| `generated/experimental/dragon_tiki_2024_runtime/shared_anim_pose_engine_ms` | Engine-timed export writes `engineTimeMs = rawFrameTime * 33.333332`; raw key `250` corresponds to about `8333.333 ms`. | The keyframe JSON now preserves the IDA-proven timing scale for truthful viewer playback diagnostics. | CONFIRMED | Use this export when testing real-time playback once clip windows are found. |
| `generated/experimental/dragon_tiki_2024_runtime/shared_anim_pose_normalized_track` | Normalized-track export stretches each track over `0..1`, creating `48` diagnostic frames and avoiding early clamping from mixed per-channel key ranges. | This explains why earlier GIFs moved briefly then settled: many tracks end far before raw key `250`; normalized output is a diagnostic, not final game timing. | EXPERIMENTAL | Keep normalized GIFs as regression/motion-shape checks while using engine-ms for truth. |
| current DML IDB `0x7FF72094FDD2-0x7FF72094FE7F` | Case-12 dispatch now has exact selector-to-slot proof in the active DML imagebase: selector `0/1/2/3 -> vtable + 0x168/+0x170/+0x178/+0x180`. | The cooked descriptor byte at `+0x10` is a real runtime mode field selecting among concrete handler-family slots, not a vague heuristic. | CONFIRMED | Recover the constructor that writes the cooked `+0x10` selector byte for ordinary shared tracks. |
| current DML IDB `0x7FF72095D940` plus `0x7FF72094CFA0` | `0x7FF72095D940` reads three float lanes from a row/stride-indexed record and reconstructs the fourth quaternion component; `0x7FF72094CFA0` wraps the same family and forwards it to the local-rotation sink at `+0x158`. | This is now the strongest executable-side candidate family for the bulk ordinary gorilla rotation descriptors that advertise `stride = 16`. | STRONGLY SUPPORTED | Prove that one cooked runtime descriptor for `head_ctrl-node-rotation` or another ordinary rotation track selects this family at runtime. |
| current DML IDB `0x7FF72095D7B0` plus `0x7FF72094CF20` | `0x7FF72095D7B0` uses the same qword field split as `0x95D940`: low32 = row/table index, bits `32..47 = baseOffset`, bits `48..63 = stride`. It reads two consecutive `16`-byte quaternion samples from the same row/stride stream, reconstructs `w` for both, then blends them through `0x7FF720955D60`; `0x7FF72094CF20` forwards the blended result to the same local-rotation sink. | The ordinary `stride = 16` gorilla-body rotation family now has both a single-sample reader and a playback/interpolated reader in the live executable, which is a much tighter match for `head_ctrl-node-rotation` than earlier broad guesses. | STRONGLY SUPPORTED | Recover which cooked selector/class routes ordinary shared rotations into the single-sample sink variant versus the blended playback variant. |
| current DML IDB `0x7FF72095CC30` / `0x7FF72095D610` with wrappers `0x7FF72094D110` / `0x7FF72094CEC0` | `0x95CC30` decodes a compact quaternion using packed unsigned `24-bit`-like component fields plus an explicit sign bit for `w`, while `0x95D610` does the same broad job from packed signed-16-bit component fields with a sign/control bit. Their wrappers forward directly to the local-rotation sink. | The runtime rotation system includes compact single-sample quaternion families beyond the direct-float `stride = 16` family. These are concrete alternatives that the cooked descriptor builder could choose for non-ordinary tracks. | CONFIRMED | Use these as contrast families when ordinary `stride = 16` channels are separated from compact packed channels. |
| current DML IDB `0x7FF72095C650`, `0x7FF72095E160`, and `0x7FF72095E300` | `0x95C650` reconstructs a 3-vector plus scalar angle-like value, then `0x95E260` / `0x94C8B0` convert that axis-angle pair into a quaternion via `sinf/cosf`. `0x95E160` does the single-sample axis-angle path, and `0x95E300` does the scaled/packed variant. | The engine has a distinct axis-angle-to-quaternion family in addition to direct quaternion families. This means “rotation track” in runtime code is format-polymorphic, which is important when we eventually generalize beyond the ordinary gorilla-body family. | CONFIRMED | Distinguish which shared-body or FX tracks use axis-angle families instead of direct quaternion storage. |
| `generated/experimental/dragon_tiki_2024_runtime/shared_anim_pose/shared_anim_debug_pose.json -> debugPoseExport.unresolvedSharedTrackNodeCount` | The export reports `52` unresolved shared-track nodes after removing the three decoded tail bones. | We now have an explicit machine-readable frontier between solved animation application and unsolved playback. | CONFIRMED | Use this unresolved-node inventory to prioritize the next executable-side decoder work. |
| current implementation boundary | The probe now resolves the shared family, proves exact node binding, and emits experimentally validated local tail quaternions, but it does not yet skin or render an animated mesh. | Shared animation assignment and one special packed rotation path are solved enough for viewer plumbing, while full animated playback still depends on skinning and broader decode coverage. | CONFIRMED | Build a debug pose export path before attempting full animated rendering. |

## IDA Playback Sink Update - 2026-06-12

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| evaluator-family vtable blocks around `0x7FF722D333B0` | Repeated `0xB0`-sized blocks contain the evaluator methods. The live vptr alignment is block `+0x18`: `+0x60/+0x68` are direct/blended output methods, `+0x70/+0x78` are time wrappers, and `+0x90/+0x98` are higher-level wrappers such as `0x7FF7209B4E80` and `0x7FF7209B4F60`. | The evaluator-family table is now aligned well enough to reason about the virtual calls made after `0x7FF7209F9CF0` samples key/alpha. | CONFIRMED for table bytes, STRONGLY SUPPORTED for vptr alignment | Find the constructor/runtime descriptor that stores this vptr into the cooked evaluator object. |
| `0x7FF72094CFA0` / `0x7FF72094CF20` | Direct and blended rotation output methods both end by calling receiver virtual `+0x158`; the blended path reconstructs two quaternions and blends them through `0x7FF720955D60`. | Shared body-type rotation tracks have a proven playback-side local-rotation sink family. | CONFIRMED | Tie `head_ctrl-node-rotation` row `0x41` to this concrete evaluator family. |
| `0x7FF72094AE80` / `0x7FF72094AE00` and siblings | Direct and blended translation output methods call receiver virtual `+0x168`. | Shared body-type translation tracks have a proven playback-side local-translation sink family. | CONFIRMED | Tie `body_root-node-translation` row `0x0D` to this concrete evaluator family. |
| `0x7FF72094B090` / `0x7FF72094B010` and siblings | Direct and blended scale output methods call receiver virtual `+0x148`. | Shared body-type scale tracks have a proven playback-side local-scale sink family. | CONFIRMED | Tie one ordinary Gorilla scale track to this concrete evaluator family. |
| current viewer implication | File-side shared tracks already export local TRS; IDA now proves live evaluator families apply local scale/rotation/translation through the same receiver interface used by loaded nodes. | We are close to a correct debug playback loop, but still need the cooked descriptor/object bridge and clip/subclip time source before calling the viewer's animation fully game-accurate. | STRONGLY SUPPORTED | Patch the viewer experimentally to use the same TRS order while continuing to trace descriptor construction and clip state selection. |
