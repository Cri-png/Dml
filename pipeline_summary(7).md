# Pipeline Summary

## Current Best-Supported End-To-End Model

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| `BUD` archives | Existing extraction scripts and prior inspection already proved: `BUD\\0` header, Zstandard-compressed index, per-entry `filename`, `compressed_size`, `uncompressed_size`, and consecutively stored payloads. | BUD is the outer archive/distribution layer and is no longer the main reverse-engineering blocker. | CONFIRMED | Only revisit if a still-missing dependency like `shadow_dragon.tga` must be recovered from another archive. |
| runtime BDAE dependency names | Metal Seahorse runtime names `Dragon.bdae`, `Dragon_Eye.bdae`, `dragon_metal_seahorse.tga`, `eye_metal_seahorse.tga`, `shadow_dragon.tga`. Crystal Lady runtime names `Dragon.bdae`, `Dragon_Eye.bdae`, `TextureAnim.bdae`, `dragon_crystal_lady.tga`, `eye_crystal_lady.tga`, `fx_spark_alpha.tga`, `shadow_dragon.tga`. | A dragon runtime BDAE directly names both secondary BDAE resources and the texture assets it expects to bind. | CONFIRMED | Tie each reference record to the executable-side resource resolution path. |
| `data.bud -> dragon_types.json` plus `dragon_body_type_specific_data.json` | Per-dragon data entries expose `BODY_TYPE`, mesh stems, and egg mesh; body-type entries expose shared `ANIMATIONS` arrays. `D_TIKI_2024` resolves to `BODY_TYPE = GORILLA`, which resolves to `dragon_gorillabody_anim` / `dragon_gorillabody_baby_anim`. | Shared dragon animation selection is data-driven through per-dragon `BODY_TYPE` assignment, not by reusing another dragon's body mesh file. | CONFIRMED | Trace the executable-side JSON consumer if code proof is needed in addition to file proof. |
| current DML IDB `0x7FF720CF3EE0` | Executable parser reads `HABITAT_MOVE_DIST_RANGE`, `HABITAT_IDLE_TIME_MS_RANGE`, `HABITAT_NUM_ANIM_RANGE`, `ANIMATIONS`, and `BODY_TYPE`; each animation string is copied into a `0x20`-stride array and `.bdae` is appended. | The game consumes body-type JSON as the source of exact shared animation BDAE names. | CONFIRMED | Trace caller `0x7FF720AED7C0` forward if we need the precise async load point. |
| `data.bud -> localization_<lang>.json` plus `localization_override_<lang>.json` | The base asset index already proves the shipped game carries per-language localization tables plus tiny override tables in the same archive family as `dragon_types.json`. | Dragon display names should be modeled as a separate localization layer on top of authoritative dragon IDs and asset stems. | STRONGLY SUPPORTED | Extract `localization_en.json` and identify how one known dragon ID maps to its displayed name. |
| local extracted `player_names.json` plus executable string `player_names.json` | The locally extracted file is a `NICKNAME_###` multilingual table, and the executable references `player_names.json` separately from the generic localization callback path. | Nicknames/custom names are a separate subsystem from the shipped dragon species-name table. | CONFIRMED | Keep species-name lookup and nickname/custom-name handling separate in the viewer/browser design. |
| `generated/experimental/dragon_tiki_2024_runtime/shared_anim_probe/probe_shared_animation_application.json` | The current probe resolves Tiki's shared anim family from authoritative data, confirms `55/55` exact node-name bindings from `dragon_gorillabody_anim.bdae` onto the Tiki adult mesh skeleton, and emits an experimentally validated stride-4 local tail rotation patch for `Tail_bone01/02/03-node`. | The project now has a real file-side handoff point from per-dragon mesh selection into inherited shared body-type animation assignment, even though full generalized animated playback is not yet implemented. | CONFIRMED | Broaden from the tail patch into a larger debug pose export or executable-side decode bridge. |
| `generated/experimental/dragon_tiki_2024_runtime/shared_anim_probe/dragon_gorillabody_anim_descriptors.json` | Corrected file-side inventory shows the gorilla shared anim uses `inputRel -> 0x18 input/time descriptor` and `outputRel -> 0x18 output/value-row descriptor`. All `66` tracks use time mode `3`; output families are `50` ordinary rotations (`valueKind=4`, `stride=16`), `10` ordinary translations (`valueKind=3`, `stride=12`), `2` ordinary scales (`valueKind=3`, `stride=12`), and `4` scalar/special tracks (`valueKind=1`, `stride=4`). | This supersedes the older qword-only `outputRel + 0x24` sentinel interpretation. The shared gorilla body anim is descriptor-row driven, not a flat unresolved payload. | CONFIRMED | Keep the corrected descriptor model as the viewer/playback foundation. |
| `dragon_gorillabody_anim.bdae` ordinary anchors | `head_ctrl-node-rotation` now has exact descriptor qword `0x0010000000000041` (`stride = 16`, `row = 0x41`), and `body_root-node-translation` has exact descriptor qword `0x000C00000000000D` (`stride = 12`, `row = 0x0D`). | The shared-body animation bridge now has concrete ordinary track anchors on disk, not only family-level summaries or special-case tail evidence. | CONFIRMED | Match these two anchors against runtime decoder/sampler families. |
| `dragon_gorillabody_anim.bdae` row table candidate | `inspect_shared_anim_descriptors.py --find-row-table` finds row table base `0x6604`, validates all `66/66` tracks, and uses row entry formula `sample = rowTableBase + rowIndex * 8 + s32(row + 0)`. `head_ctrl-node-rotation` row `0x41` decodes normalized XYZW quaternion samples; `body_root-node-translation` row `0x0D` decodes vec3 samples. | Ordinary shared-body local transform channels are now file-side decodable from the shared anim BDAE. | STRONGLY SUPPORTED | Keep using this formula while resolving the cooked runtime descriptor builder. |
| `generated/experimental/dragon_tiki_2024_runtime/shared_anim_pose/dragon_gorillabody_anim_keyframes.json` and successors | Exporter decodes `62` local transform tracks (`50` rotations, `10` translations, `2` scales), keeps the `4` `valueKind=1/stride=4` scalar/special tracks unresolved, and now writes IDA-proven `engineTimeMs = rawFrameTime * 33.333332` for mode-3 rows. | The viewer can now consume broad descriptor-row local TRS data with correct time-scale metadata, though skinning/application remains experimental. | STRONGLY SUPPORTED | Use engine-ms export for truthful playback diagnostics and normalized-track export only as a motion-shape microscope. |
| `generated/experimental/dragon_tiki_2024_runtime/shared_anim_pose_engine_ms` | Engine-timed export reaches `maxEngineTimeMs ~= 8333.333` for raw key `250`; many channels still naturally clamp earlier because their row times end at raw keys like `5`, `10`, `25`, `108`, or `201`. | The “moves for a bit then tweaks/holds” GIF behavior is best explained by mixed per-channel time ranges plus missing clip/subclip windows, not by dead keyframe data. | STRONGLY SUPPORTED | Locate clip/state metadata that decides the real sampled time range. |
| current DML IDB `0x7FF7209FBC20` | Decompilation compares the live animation time against selected clip entry `+0x08/+0x0C`, binary-searches the clip table when outside the current range, clamps the integer time into the chosen clip window, and calls `0x7FF7209F2F90(..., selectedClipEntry, clampedTime, ...)`. | The game has a real clip-window selection/clamp layer between named clip lookup and row evaluation. The viewer's `clip-local-ms` output is a useful diagnostic remap, not final game-equivalent timing. | CONFIRMED for clip selection/clamping, STRONGLY SUPPORTED for remaining remap gap | Resolve `0x7FF7209F2F90` and the returned selected-clip data object to prove exact row-time mapping. |
| `generated/experimental/dragon_tiki_2024_runtime/shared_anim_pose_normalized_track` | Normalized diagnostic export stretches each track's own key row over `0..1`, emits `48` frames, and produces steadily changing skeleton/skinned diffs. | The decoded keyframe values are alive; normalized output is useful for regression tests but is not game timing. | EXPERIMENTAL | Keep separate from engine-ms output in the future viewer. |
| runtime BDAE container | Both runtime samples are `BRES` containers with the same header model and section order. | BDAE is currently best modeled as a BRES-family asset container rather than a separate unrelated format. | CONFIRMED | Identify the exact executable-side BDAE/BRES loader entrypoint that recognizes the header. |
| base body/eye/effect BDAEs | `Dragon.bdae`, `Dragon_Eye.bdae`, and effect/material BDAEs are small, shader/profile-heavy, and have no large declared post-record payload arena. | These files are base render/profile/effect graphs, not the main per-dragon transform payloads. | STRONGLY SUPPORTED | Keep tracing runtime references into these secondary BDAEs in code. |
| embedded-vs-standalone FX sample pair | `dragon_paper_lantern.bdae` is a mesh/skin-bearing BRES with direct references to `TextureAnim.bdae`, multiple FX textures, and additive/premultiplied profile names, while `fx_buble_dragon_buble.bdae` is a tiny standalone BRES naming `fx_soap_bubble.tga` and `node-node-gnps_emitter` but no mesh bridge. | The viewer should support at least two FX asset patterns: embedded dragon-side FX/material passes and separate standalone `fx_*.bdae` effect graphs. | STRONGLY SUPPORTED | Keep comparing more dragons with dedicated effect-looking passes and map their binding records. |
| dragon textures | All four examined body/eye `.tga` files are actually DDS / BC3-DXT5 textures with standard 128-byte DDS headers. | The texture stage in the offline viewer can use magic-based DDS decoding immediately for the known dragon body/eye textures. | CONFIRMED | Validate with a quick offline preview implementation. |
| current DML UI bridge `0x7FF721C3FE60` and callbacks `0x7FF721C455E0`, `0x7FF721C45840`, `0x7FF721C45E50` | The executable exposes sibling UI callbacks `setDragonPortrait`, `setFullBodyDragonImage`, and `setImageFromBdae`, and all three submit render-style objects through `0x7FF720A6AD40`. | The game already has a UI-facing asset-image bridge that could inform a future viewer/browser design, even though it does not by itself solve animation playback. | STRONGLY SUPPORTED | Identify the exact accepted string key for each callback and the final consumer behind `0x7FF720A6AD40`. |
| `viewer_prototype/dml_viewer_session.py` and generated Tiki outputs | The new session wrapper emits clean static preview PNGs, cropped transparent PNGs, OBJ/glTF, named shared-clip transform exports, and optional skeleton/skinning diagnostics. For Tiki 2024 it uses `D_Tiki_2024-mesh`, `437 x 24` vertices, two primitive groups covering `0..436`, and XZ projection. The clean static output matches the expected Masked/Tiki body silhouette. | The viewer can now safely default to static textured runtime mesh display while keeping animation/skeleton/skinning as separate experimental layers. This prevents the unresolved skinning-space bug from crushing an otherwise correct dragon render. | CONFIRMED for static output generation and validation checks, EXPERIMENTAL for animation overlays | Wire the external GUI to this facade first; keep `--experimental-skinning` hidden behind an explicit debug option until the engine-equivalent skinning compose is proven. |

## Runtime BRES Structure

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| runtime `strings_end` root-like record | Metal Seahorse root-like record at `0x2CE8` has `+0x40 = 52`, `+0x44 = 236`. Crystal Lady root-like record at `0x2FD0` has `+0x40 = 43`, `+0x44 = 236`. | The same runtime metadata structure exists in both samples. | CONFIRMED | Find executable-side reads of this structure. |
| runtime root-like `+0x40` | The field equals the exact named transform-track count in both samples. | `root_like + 0x40` is now the strongest proven candidate for total transform-track count. | CONFIRMED | Promote from structural confirmation to parser confirmation once code reads it. |
| runtime transform-track array | In both runtime samples, the visible transform-track records begin at `records_start + 0x10`, are contiguous, use `0x28` stride, and count exactly matches the root-like `+0x40` field. | The runtime file has a stable contiguous track-record array immediately inside the records region. | CONFIRMED | Map the 16-byte preamble and the owning record type. |
| runtime track payload targets | The `inputRel`/`outputRel`/`extraRel` pointers for visible transform tracks point into a compact early slice near the start of the runtime payload arena in both samples. | Track metadata and track numeric payloads are physically separated. | STRONGLY SUPPORTED | Backward-trace one payload slice through executable decode code. |
| runtime mesh-owner records | In both runtime samples, the record owning `...-mesh` points to three deeper payload blocks (`stage0`, `stage1`, `skinStage`) and the nearby `...-mesh-skin` owner shares the same structured region. | Mesh/skin payload is now tied to concrete BRES record ownership rather than only to a late-file arena scan. | CONFIRMED | Find the executable-side mesh reader for this record family. |
| Crystal stage-0 split | Crystal stage 0 now separates cleanly into one first external target at `0xC098` and five later descriptor-owned targets at `0xF908`, `0x10088`, `0x104F8`, `0x10738`, `0x10748`. The first target is exactly `516 x 28` bytes; the later targets are small-integer-heavy and several sizes are exact multiples of `6` bytes. | The runtime mesh section is no longer best described as “float blob plus integer blob”. It is strongly supported as one interleaved vertex-like stream plus several primitive-group payloads. | STRONGLY SUPPORTED | Tie one primitive-group target to the executable index unpack path. |
| mesh-owned stage pointers | The stage-0 primary pointer in both samples reaches float-rich interleaved data, later stage-0 pointers reach dense small-integer tables, and skin-stage pointers reach weight-like and binding-like regions. | A substantial part of the deep runtime payload arena is now strongly supported as mesh/skin payload, and the likely render target is a flat or 2.5D skinned sprite-mesh rather than a fully volumetric mesh. | STRONGLY SUPPORTED | Confirm vertex/index/weight semantics in the executable-side loader and quantify depth distribution across more samples. |
| Metal-vs-Crystal payload-arena comparison | Nearly the entire `~1.13 MB` runtime size gap comes from the Metal Seahorse `data_start..EOF` region, and that excess is now reached through mesh-owned stage pointers rather than only by global scans. | The large Metal runtime is best modeled as mostly mesh/skin-owned payload volume, not transform-track metadata alone. | STRONGLY SUPPORTED | Prove ownership of the late payload ranges through the executable-side mesh reader. |

## Executable-Side Bridge

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| `0x7FF7A7ECE940` | This function creates a node-like object via a virtual method at `(*(*(a1+8)) + 0x130)`, calls `sub_7FF7A7ECEB20(a1, &temp, a3, a4, &node_ptr)`, then calls the created node's `+0xC8` method and `Scene_FinalizeLoadedNode`. | `sub_7FF7A7ECEB20` is operating during loaded-node construction/finalization, not obviously during per-frame playback. | CONFIRMED | Follow `Scene_FinalizeLoadedNode` and the created node type if final scene-graph semantics are needed. |
| `0x7FF7A7ED0BB0` | This caller walks child-like records in `96`-byte steps and recursively calls `sub_7FF7A7ECEB20(...)` for each one before attaching them to the current loaded object. | The executable is recursively constructing a hierarchy from BRES-derived records. | STRONGLY SUPPORTED | Map the `96`-byte child record layout on disk or in loaded memory. |
| `0x7FF7A7ECFC9C-0x7FF7A7ECFDCD` | Case 12 of `sub_7FF7A7ECEB20` does exact `strcmp` name matching against a runtime descriptor array: count at `owner+0xBC`, rel/base at `owner+0xC0`, stride `0x18`, then reads a byte at matched descriptor `+0x10`. | A file-derived name is bridged into a cooked `0x18` runtime descriptor table before decode dispatch. | CONFIRMED | Identify where that `0x18` table is built/populated. |
| `0x7FF7A7ECFDD2-0x7FF7A7ECFE7F` | Selector values `0`, `1`, `2`, `3` dispatch to handler vtable slots `+0x168`, `+0x170`, `+0x178`, `+0x180`, which map to `EDCC30`, `EDDA10`, `ECD110 -> EDCC30 -> consumer`, and `ECD090 -> EDDA10 -> consumer`. | The runtime descriptor byte at `+0x10` is a real small mode/type field used to choose among packed-value decode variants and whether they immediately forward to a consumer. | CONFIRMED | Recover the exact source field or constructor logic that assigns those values. |
| current DML imagebase `0x7FF72094FDD2-0x7FF72094FE7F` plus handler bases `0x7FF722D332B8/0x7FF722D33418/0x7FF722D334C8/0x7FF722D336D8/0x7FF722D33788` | In the current DML IDB, selector `0/1/2/3` still maps to `+0x168/+0x170/+0x178/+0x180`, but we now have the concrete handler-family table behind those slots. Across five rotation-handler classes, selector `3` routes into playback-style sampler `0x7FF7209B53E0`, while selectors `0/1/2` route into direct decode and decode-plus-sink variants such as `0x7FF72095C810`, `0x7FF72095CA20`, `0x7FF72095D2B0`, `0x7FF72095D530`, and `0x7FF72095D7B0`. | The cooked selector byte is no longer just a four-way mystery. It chooses among concrete decode families, and on several classes selector `3` now clearly points at a playback-capable sampler path. | CONFIRMED | Recover the cooked selector byte for one ordinary shared-body track to determine which branch its runtime evaluator actually uses. |
| `0x7FF7A7ECD090`, `0x7FF7A7ECD110`, `0x7FF7A7ECCF20`, `0x7FF7A7ECCFA0` | These wrappers decode quaternion-like values into a temporary 16-byte buffer and then call a receiver object's virtual method at `vtable + 0x158` (`344`). | Decoded quaternion output is consumed by a caller-supplied runtime object through a stable quaternion-sink interface. | CONFIRMED | Identify the receiver class and the semantic meaning of its `+0x158` method. |
| `0x7FF7A7ED005B-0x7FF7A7ED00AF` inside `sub_7FF7A7ECEB20` | The same runtime object in `rdi` is called directly with `+0x168` using a vec3, `+0x158` using a quaternion, and `+0x148` using another vec3 loaded from the current record stream. | The `+0x158` sink is now best modeled as a node/local-rotation setter inside load-time scene construction. | STRONGLY SUPPORTED | Prove that the packed decode path reaches the same concrete receiver class. |
| `0x7FF7A7EC7DC0 -> 0x7FF7A7ED2090 -> receiver+0x158` | One apparent `+0x158` implementation builds or accumulates a quaternion from multiple inputs and then forwards that quaternion to a second receiver object's `+0x158`. | The current call chain still contains at least one adapter layer between decode helpers and the deepest concrete transform sink. | CONFIRMED | Follow the second-layer receiver to the final concrete transform write. |
| `0x7FF7A7ED17B0` | Reconstructs packed components and computes the fourth component via a square-root expression consistent with quaternion reconstruction. | Compact quaternion decode is proven. | CONFIRMED | Tie one on-disk channel payload directly to the exact packed input buffer passed here. |
| current DML `0x7FF72095D940` and wrapper `0x7FF72094CFA0` | `0x7FF72095D940` reads three float lanes from a row/stride-indexed record, computes the missing fourth quaternion component by square root, and `0x7FF72094CFA0` forwards the resulting quaternion to the local-rotation sink. | This is now the strongest executable-side candidate for the bulk ordinary gorilla rotation descriptors that advertise `stride = 16`. | STRONGLY SUPPORTED | Prove that `head_ctrl-node-rotation` or another ordinary rotation track selects this family via a cooked runtime descriptor. |
| current DML `0x7FF72095D7B0` and wrapper `0x7FF72094CF20` | `0x7FF72095D7B0` uses the same qword field split as `0x95D940` (`low32=row`, `bits32..47=baseOffset`, `bits48..63=stride`), reads two consecutive direct-float quaternion samples from that row/stride stream, reconstructs `w` for both, and blends them through `0x7FF720955D60`; `0x7FF72094CF20` forwards the blended result to the same local-rotation sink. | The ordinary `stride = 16` gorilla-body family now has both a single-sample reader and a playback/interpolated reader in the live code, which is a materially stronger bridge for `head_ctrl-node-rotation` than the earlier single-function hypothesis. | STRONGLY SUPPORTED | Recover which cooked selector/class routes ordinary shared rotations into the single-sample sink variant versus the blended playback variant. |
| current DML `0x7FF7209B5030 / 0x7FF7209B51D0` with `0x7FF7209B42D0..0x7FF7209B4C60` | These helpers compute interpolation factor `t` from two key-time values stored as `u8`, `i16`, `u16`, or `i32`-like samples. For the mode-3 helper used by Gorilla rows, `u16` key times are multiplied by `33.333332` before interpolation. | The engine now has a proven playback-style key-time interpolation layer rather than only load-time transform adapters, and the shared Gorilla rows have a confirmed time scale. | CONFIRMED | Find the higher-level caller that repeatedly feeds advancing time into this sampler layer. |
| current DML `0x7FF7209F9CF0` | Disassembly proves the live `float` argument is copied from `xmm1` into `xmm7`, then passed as `xmm3` into `0x7FF7209B51D0` at `0x7FF7209F9E35`; later code uses returned key/alpha state to call virtual evaluators. | Descriptor-row sampling is now strongly tied to runtime animation evaluation/application, not just load-time decode. | STRONGLY SUPPORTED | Resolve the later virtual evaluator methods to identify the exact property/node sinks. |
| current DML `0x7FF7209F9450`, `0x7FF7209F9CF0`, `0x7FF7209FAE30`, `0x7FF7209FB5C0`, and `0x7FF7209BCCF0` | These broader evaluators accept live float parameters, route through `0x7FF7209B51D0` or `0x7FF7209B5030`, and continue through larger per-property/per-output logic rather than returning immediately to a load-time node builder. | The proven sampler layer is embedded in a wider animation-evaluation subsystem, so the project is now one bridge away from generalized debug playback rather than still being only a load-time decoder hunt. | STRONGLY SUPPORTED | Recover the owning runtime animation object and the caller that advances this float parameter frame to frame. |
| current DML IDB remap: `0x7FF720955D60` | This helper accepts two decoded quaternions plus blend weights `1-t` and `t`, performs normalized quaternion interpolation, and writes one output quaternion. Multiple packed decode families feed into it. | The runtime has a shared quaternion blend/evaluate stage reused across several animation sample formats rather than one monolithic decoder. | CONFIRMED | Use this function as the convergence point when classifying additional shared-body rotation channels. |
| current DML IDB remap: `0x7FF72095C810` | This decoder reads two quaternion samples from a row-table/datarel scheme using signed 8-bit packed component values, applies per-component base/scale arrays, reconstructs the missing fourth component with a square-root path, then blends through `0x7FF720955D60`. | One concrete shared-body rotation family stores quaternion samples as packed signed-byte `xyz` with implicit `w`. | CONFIRMED | Map a cooked runtime descriptor for one unresolved shared-body rotation track into this family. |
| current DML IDB remap: `0x7FF72095CA20` | Same broad scheme as `0x7FF72095C810`, but reads signed 16-bit packed component values at offsets `+4/+6/+8/+10` before `w` reconstruction and blending. | A second shared-body rotation family stores quaternion samples as packed signed-16-bit `xyz` with implicit `w`. | CONFIRMED | Differentiate where the descriptor builder chooses the 16-bit family instead of the 8-bit family. |
| current DML IDB remap: `0x7FF72095CC30` and `0x7FF72095D610` | `0x95CC30` decodes a compact quaternion from packed unsigned `24-bit`-style component fields and an explicit sign bit for `w`; `0x95D610` does the same broad job from packed signed-16-bit fields plus a sign/control bit. Both have direct sink wrappers nearby. | The runtime has concrete compact single-sample quaternion families beyond the ordinary direct-float `stride = 16` family. | CONFIRMED | Use these as contrast families when separating ordinary shared-body rotations from more compact packed tracks. |
| current DML IDB remap: `0x7FF72095C650`, `0x7FF72095E160`, and `0x7FF72095E300` | `0x95C650` reconstructs a 3-vector plus scalar angle-like value, then `0x95E260` / `0x95E160` / `0x95E300` convert that axis-angle representation into a quaternion through `sinf/cosf`. The family exists in both single-sample and scaled/packed forms. | Rotation data in DML is format-polymorphic: direct quaternion, compact quantized quaternion, and axis-angle families all sit behind the same sink interface. This explains why the cooked descriptor builder is still the decisive missing bridge. | CONFIRMED | Distinguish which shared-body or FX tracks use axis-angle families instead of direct quaternion storage. |
| current DML IDB remap: `0x7FF7209512D0 -> 0x7FF72095D2B0` and `0x7FF720951540 -> 0x7FF72095D530` | These helper/wrapper pairs decode two temporary quaternions into an `8-float` buffer and then blend them through `0x7FF720955D60` before forwarding the result to the `+0x158` rotation sink. `0x7FF7209512D0` uses packed signed-byte `xyz`; `0x7FF720951540` uses packed signed-16-bit `xyz`. | The engine exposes multiple sibling “decode sample pair -> blend -> apply rotation” families behind the same rotation-consumer interface. | CONFIRMED | Recover which descriptor-selector/object class routes a given shared anim track into each helper family. |
| current DML IDB remap: `0x7FF720951CA0 -> 0x7FF72095DAF0` | This helper decodes two quaternions from `16-byte` sample records carrying four explicit `24-bit` quantized component fields, one every three bytes starting at `+4/+7/+10/+13`, then the wrapper blends the pair through `0x7FF720955D60`. Unlike the `xyz` families, this path stores all four components explicitly and does not reconstruct `w` by square root. | This is the strongest current engine-side candidate family for the experimentally decoded Tiki tail `16-byte` stride-4 packets. | STRONGLY SUPPORTED | Prove that one tail-bone cooked runtime descriptor reaches `0x7FF72095DAF0` / `0x7FF720951CA0`. |
| `0x7FF7A7F66130`, `0x7FF7A7F66510`, `0x7FF7A7F650F0` | This family parses per-parameter properties `sem`, `tc`, `id`, `inst`, `dt`, enforces `tc` as a texture-coordinate property, and performs sibling matching around `_transform` names while checking texcoord scale/offset consistency. | The engine has an explicit vertex-attribute / texcoord semantic layer. That materially supports the flat textured mesh interpretation and reduces the chance that the float-rich stage-0 payload is unrelated numeric data. | STRONGLY SUPPORTED | Recover the semantic enum table behind `sub_7FF7A7F599D0` and tie a runtime-BRES material/profile record into this path. |
| current DML `0x7FF7207F2250 -> 0x7FF7207F43A0 / 0x7FF7207FBAE0 / 0x7FF7207EC7D0` | `0x7FF7207F2250` tokenizes a declaration string through `0x7FF720801680`, checks whether any resulting `u16` tokens fall in the special `0x600..0x8FE` range, classifies the stream with `0x7FF7207F43A0`, groups segment IDs with `0x7FF7207FBAE0`, and then emits paired `96`-byte declaration records through `0x7FF7207EC7D0`. `0x7FF7207F1FF0` reruns the grouping pass over already-built segments. | The live engine has a concrete declaration-token pipeline between mesh/declaration strings and emitted runtime mesh records. This is now a proven executable-side bridge for the vertex/semantic layer, even though it still stops short of the final matrix-palette compose step. | CONFIRMED | Recover the exact token IDs for the skin-related semantics and the downstream consumer that turns the `96`-byte records into live vertex declarations or GPU input layouts. |
| current DML `.rdata` alias table near `0x7FF722801AC0` and `0x7FF722803220` | Pointer/code pairs now prove both `bone_matrices -> 0x0C` / `bonematrices -> 0x0C` and `weight_mask -> 0x0D` / `weightmask -> 0x0D`. | `bone_matrices` and `weight_mask` are real engine-side shader-parameter aliases for a concrete paired code set used by the runtime, rather than stray debug strings. | CONFIRMED | Follow the parameter manager that consumes this alias table and trace where codes `0x0C/0x0D` are uploaded during skinned rendering. |
| current DML typed parameter accessors `0x7FF720943B50/0x7FF7209443C0` and `0x7FF720941380/0x7FF720941C20` | `0x943B50` reads descriptor type `14` and copies exact `64` bytes; `0x9443C0` writes the same type. `0x941380` reads descriptor type `11` and copies exact `16` bytes; `0x941C20` writes the same type. | The render-parameter system now has proven storage classes for matrix-array type `14` and `float4`-array type `11`. | CONFIRMED | Tie these typed parameter classes to the skinned draw setup path and eventually to the final GPU upload. |
| current DML `0x7FF720A60020` | This render-side function resolves parameter code `0x0C` with `sub_7FF72093F470(..., 12, ...)`, resolves code `0x0D` with `sub_7FF72093F470(..., 13, ...)`, composes one temporary `4x4` matrix per source entry, writes each matrix through `0x9443C0`, then writes a companion `float4(1,1,1,1)` through `0x941C20` if the `0x0D` slot exists. | This is the strongest current executable-side skinning anchor: a live render setup path that stages `bone_matrices` and `weight_mask` together via the typed parameter system. | CONFIRMED for the parameter pair and writes, STRONGLY SUPPORTED for the skinning role | Identify the owning object/class for `0xA60020` and the later consumer that turns the staged parameter set into GPU-visible state. |
| current DML `0x7FF720A5CED0`, `0x7FF7209902C0`, and `0x7FF7209D6F10` | These sibling functions route through the same typed parameter framework. `0x9902C0` and `0x9D94C0` explicitly default missing type-14 parameters to identity matrices and missing type-11 parameters to constant vectors; `0x9D6F10` copies matching parameter arrays and handles type `0x0E` through `0x943B50/0x9443C0` and type `0x0B` through `0x941380/0x941C20`. | The matrix/weight-mask path is part of a general typed render-parameter framework rather than a one-off special case, which materially strengthens the current skinning interpretation. | CONFIRMED | Keep tracing upward until the skinned draw object / palette owner is identified. |
| current DML `0x7FF720A606C0 / 0x7FF720A60890 / 0x7FF720A60A30 / 0x7FF720A60B90` | `0xA606C0` and `0xA60890` explicitly look up and create `"CMeshSceneNode"` entries by name. `0xA60A30` constructs an object with vtables `off_7FF722D427F0`, `off_7FF722D42A08`, `off_7FF722D42A18`, and `off_7FF722D42A28`, and `0xA60B90` is the matching destructor/finalizer for that same object family. | The `A60020` skinning-parameter family is now strongly tied to a `CMeshSceneNode`-related object cluster. This is the best current owner-class anchor for the palette setup path. | STRONGLY SUPPORTED | Recover the exact vtable slot containing `0xA60020` once broader IDA xref budget is available again, then identify the higher-level caller that drives this node family during dragon rendering. |
| current DML `.data` vtable block `off_7FF722D42790` | Exact qword scan now proves `0x7FF720A60020` lives at `.data:0x7FF722D427B0`, which is slot `+0x20` inside the earlier `off_7FF722D42790` table immediately preceding the derived `CMeshSceneNode` vtable at `off_7FF722D427F0`. Nearby slots include `0x7FF720A5FF20` at `+0x18`, `0x7FF720A5CEA0` at `+0x28`, `0x7FF720A5FE30` at `+0x30`, and `0x7FF720A5C8E0` at `+0x38`. The base destructor `0x7FF720A5FD90` resets the object vtable to `off_7FF722D42790`. | `0xA60020` is now proven to be a real virtual method in the base node-family table, not only a free helper or a table-adjacent utility. | CONFIRMED | Map the remaining base-table slots to concrete semantic roles and identify the higher-level interface that invokes them during skinned drawing. |
| current DML `0x7FF720A5CEA0` and `0x7FF720A5FFF0` | `0xA5CEA0` explicitly loads and calls the function pointer at `(*a1 + 0x20)`, which for `off_7FF722D42790` resolves to `0xA60020`. `0xA5FFF0` in turn calls virtual slot `(*a1 + 0x28)` when `a2 == 1`, and for this same table that slot resolves to `0xA5CEA0`. | There is now a concrete virtual-dispatch chain into the palette setup path: `A5FFF0 -> vtable+0x28 (A5CEA0) -> vtable+0x20 (A60020)`. This is the first proven object-method route into the `bone_matrices / weight_mask` staging logic. | CONFIRMED | Find who invokes `A5FFF0` or the corresponding interface slot during actual dragon mesh submission. |
| current DML `0x7FF720A2F750` type factory | The scene-node factory switches on literal 32-bit type IDs. The case `1752393069 / 0x6873656D / "mesh"` allocates `0x1B8` bytes and constructs the `0xA60A30` object family. Sibling cases include `2037673317 / "emty"` and `1601003875 / "cam_"`, which construct different node classes. | The executable now has a direct `"mesh"` factory case for the `CMeshSceneNode` family. This is the strongest current bridge between generic scene-node creation and the mesh/skinning node class that owns the palette setup path. | CONFIRMED | Tie the file/runtime node record type selection to this `"mesh"` factory case so the BRES mesh-owner records and executable-side node class are explicitly linked. |
| current DML `0x7FF720A61070` and `0x7FF720A606C0` | `0xA61070` lazily initializes a thread-local registry entry by calling `0xA606C0(..., "CMeshSceneNode", 1)`, then compares the requested type ID against that cached mesh-node type and either returns the current object (`a1 - 8`) or falls back to a generic registry lookup path. | The runtime has an explicit `CMeshSceneNode` type/registry layer, which is stronger evidence than a constructor string alone and helps bridge from abstract node IDs into the concrete mesh/skinning class. | CONFIRMED | Follow the registry/lookup path until the file-side node type or scene-node descriptor feeding it is identified. |
| current DML `0x7FF720A61340` | This method operates on the `CMeshSceneNode` family, pulls one child/attachment entry by index from the provider at `+376`, combines it with a render/service object at `*(*(a1+328)+64)`, pushes intermediate objects through `0x9380C0`, and then calls `0xA60ED0` to update the selected child attachment. | This is strong evidence that the same node family owning the `bone_matrices / weight_mask` staging path also participates directly in per-node child/attachment submission during rendering or state refresh. | STRONGLY SUPPORTED | Identify the caller of `0xA61340` so the node family's render/update stage can be placed more precisely in the dragon rendering pipeline. |
| current DML `0x7FF720A60A30` plus inherited ctor `0x7FF72092B680` | `A60A30` stores the incoming provider pointer at `a1+376` after inherited construction. The inherited constructor zeroes `a1+328`, `a1+344`, `a1+360`, and `a1+368`. | `CMeshSceneNode` has a real child/attachment provider at `+376`, while the render/scene service pointer at `+328` is injected later. | CONFIRMED | Find class-family write sites for `CMeshSceneNode + 0x148` rather than generic unrelated uses of the same offset. |
| current DML `0x7FF720A611C0` / `0x7FF720A61150` | `A611C0` gets provider child count via vtable slot `+24`; `A61150` fetches a child/attachment by index via provider vtable slot `+40`. | The `+376` member is a concrete provider interface, not a raw pointer to a flat array. | CONFIRMED | Tie the provider passed into `A60A30` back to parsed BRES mesh-node or attachment data. |
| current DML `0x7FF720A66A40` with `0x7FF72092D250 -> 0x7FF72098E280` and `0x7FF72092D330 -> 0x7FF72098E470` | Reads a count at `a1[1]+80` and name pointers at `a1[1]+84`, resizes a handle array at `a1[2]+8`, and resolves each name by case-insensitive scene-tree search. The two resolver paths use different node virtual name accessors at `+192` and `+184`. | The base mesh-node family has a concrete attachment-name resolution stage from descriptor-owned names to live scene-node handles. | CONFIRMED | Compare these resolved names against runtime BDAE node-binding strings and eye/body attachment targets. |
| current DML `0x7FF720A60ED0` and sibling `0x7FF7209FE590` | Both functions share the same attachment-refresh skeleton: if a per-object flag is set, call `0x934590`; otherwise call service method `(*a2 + 104)` with the current attachment object and possibly replace the handler pointer stored at `*a3 + 56`. | Attachment/handler refresh is a shared node-family behavior, not a one-off helper unique to Tiki or one draw path. | STRONGLY SUPPORTED | Identify the concrete type of the service object passed as `a2` and the handler object stored at attachment `+56`. |
| current DML `0x7FF72092F060` | When visibility-like state bits at `a1+352` cross the `(bits & 0x18) == 24` threshold and `a1+328` is non-null, the function walks a list at `service + 96 .. service + 104` and calls each listener's virtual `+56` with the node. | The `+328` pointer behaves like a non-owned scene/service hub with an observer/listener range, not like a plain mesh payload pointer. | CONFIRMED | Identify the concrete class of the `+328` hub and the listener type behind virtual `+56`. |
| current DML `0x7FF720A2F750` post-create hook | After constructing a `"mesh"`, `"emty"`, or `"cam_"` node object, the factory checks `if (*a2 && a4)` and calls virtual slot `+200` on `a4` with the newly created node pointer. | Newly created scene nodes are immediately handed to an external owner/context object after construction; this is a strong candidate stage for later injection of non-owned service pointers such as `+328`. | STRONGLY SUPPORTED | Identify the concrete type behind `a4` and its slot `+200` implementation. |
| current DML `Scene_CreateObjectFromAssetName` at `0x7FF720CAD790` | After `Scene_CreateNodeFromBdae(...)` returns a node, this function immediately calls the parent's virtual `+200` slot with the new node pointer. The base `+200` implementation is `0x7FF72092C390`, which forwards into the shared attach method `0x7FF72092C5C0`. | Runtime-created BDAE nodes are attached into an existing scene parent immediately after creation; this gives a concrete engine path from asset creation into the attach/service propagation layer. | CONFIRMED | Tie one live caller's parent argument to a concrete scene root or already-serviced node during dragon loading. |
| current DML `0x7FF720A60C80` | Allocates a fresh `0x1B8` `CMeshSceneNode`, pulls provider-side state through the `+376` interface, constructs the node family through `0xA60A30`, then calls `0x7FF72092C7E0(new_node, source_like_parent)` before returning it. | Mesh-node duplication/creation has a family-specific wrapper, but it still routes through a generic node-copy/setup stage before any later service injection occurs. | CONFIRMED | Identify callers of `0xA60C80` and compare whether they immediately follow with owner/context hookup or attachment registration. |
| current DML `0x7FF72092C7E0` | Generic node-copy/setup helper copies common node state, clones child/related lists through virtual `+200` and `+280`, and explicitly clears `destination + 328` to zero. | The `+328` scene/service hub is definitely runtime-injected later rather than inherited from the source node or copied by the generic setup path. This is the clearest current breakpoint for the missing skinning/render ownership bridge. | CONFIRMED | Search for post-clone/post-factory owner-context methods that run after `0x92C7E0` and install `+328`. |
| current DML `0x7FF72092C390 / 0x7FF72092C3A0 / 0x7FF72092C5C0` | Virtual `+200` (`0x92C390`) and virtual `+216` (`0x92C3A0`) are wrappers over the shared attach implementation `0x92C5C0`. That implementation links the child into the parent's list, stores `child[35] = parent`, loads `parent + 328`, compares it against `child + 328`, and if they differ calls child virtual `+496`. It also notifies listeners through `0x92D920` and listener virtual `+56` when a parent service exists. | The generic attach path itself is now proven to be the main service-hub bridge: a newly attached node inherits the parent's live `+328` hub before mesh-specific skinning and attachment methods consume it. | CONFIRMED | Find the earliest attach call where the parent already has a non-null service so the first root-service installation can be identified. |
| current DML `0x7FF72092EF20` | This concrete virtual `+496` method writes `node + 328 = service`, recursively propagates the same service through descendants, calls virtual `+512` on each node, and notifies service listeners in `service + 96 .. service + 104` through listener virtual `+72`. | `0x92EF20` is the subtree service-propagation routine. Once one parent node is serviced, the whole attached dragon subtree inherits the same scene/service hub through this path. | CONFIRMED | Identify the first caller that passes a non-null service into this method directly or reaches it through `0x92C5C0`. |
| current DML `0x7FF72092C2D0 / 0x7FF72092D920` | `0x92C2D0` appends a child entry through virtual `+280` and calls `0x92D920(service, 0, node)` when `node + 328` exists. `0x92D920` fans out over listeners in `service + 96 .. service + 104` using listener virtual `+48`. | The same hub consumed later by `CMeshSceneNode` is also the generic listener backbone for node-list changes, which further supports the interpretation of `+328` as a live scene/service object rather than a render-only blob. | CONFIRMED | Identify one concrete listener class in the `service + 96 .. service + 104` range to characterize the hub more directly. |
| current DML `0x7FF7209E6DB0` | A sibling non-mesh node creation path also calls `0x7FF72092C7E0(...)` before filling its own type-specific fields. | `0x92C7E0` is a general node-copy/setup helper across node families, not a mesh-only oddity. | CONFIRMED | Use non-mesh callers as controls when identifying which later owner/context code path injects `+328` specifically for mesh nodes. |
| current DML `0x7FF720934590` | If flags at `a1+392` / `a1+372` allow it and `*(DWORD *)(a3+12) != 0`, clears bit `0x800` in `a1+1196` and calls vtable slot `+448`. | State-invalidating callback triggered when attachment-side data reports a non-zero entry count/flag. | STRONGLY SUPPORTED | Resolve the concrete implementation behind slot `+448` for one mesh-node instance. |
| current DML `0x7FF720938D30` with callers `0x7FF720A2E500` / `0x7FF720A2EAF0` | Calls vtable slot `+168`, then copies exactly `64` bytes into `a1 + 808 + index*64`. The known wrappers both pass `index = 2`. | Generic per-slot `4x4` matrix setter on a runtime object; reused by the attachment-submission/update path but not unique to `CMeshSceneNode`. | CONFIRMED | Identify the owner object of the matrix array at `+808` and determine what slot `2` represents. |
| current DML `0x7FF7209380C0` reuse set | Reused by `A61340`, `0x934AF0`, `0x937660`, several `0xC1/0xC9/0x1C1A*` render paths, and it logs transform-feedback semantic/type mismatches. | `9380C0` is a generic structure/semantic compatibility helper, so its presence in `A61340` does not by itself identify the final skinning sink. | STRONGLY SUPPORTED | Keep separating generic render-structure checks from mesh-node-specific palette and attachment logic. |
| current DML parent-class overrides `0x7FF7209FEC50`, `0x7FF7209FF060`, `0x7FF7209FE590` | The parent node family behind `0x9FE160` uses pointer-heavy attachment methods based on `a1[41]+64` and `a1[48]`, and calls back into `0x934590` / `0x9FE590` during attachment updates. `CMeshSceneNode` installs its own methods (`A61220`, `A61340`, `A60ED0`) in the corresponding derived vtable region instead. | `CMeshSceneNode` does not inherit a generic scene-node attachment path unchanged; it overrides it with mesh/provider-specific child submission logic. | CONFIRMED | Recover the exact slot mapping between the parent vtable and `off_7FF722D427F0`, then identify where the derived object gets the service pointer consumed by `A61220/A61340`. |
| current DML `0x7FF720C98AC0` with caller `0x7FF720C8D950` | The `3DMesh_Skinned` hook enumerates profile/material names, matches `Dragon`, `3DMesh_Skinned`, `Default`, and `Default_Skinned`, and conditionally binds `maskColor` plus `SkinIredescentTex` / `_Iridescent` resources. | This is a real skinned-material/profile specialization point, but it is not the missing bone-matrix palette upload or bind-space compose routine. | CONFIRMED | Keep using it for body/eye/effect material-role tracing, but continue elsewhere for the actual skinning matrix composition path. |

## What The Current Evidence Does And Does Not Prove

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| file-side `0x28` track records vs runtime-side `0x18` descriptors | In both runtime samples, the on-disk transform-track record dword at `+0x10` is consistently `1` (`outputCount`), while the executable dispatch byte at runtime descriptor `+0x10` branches on values `0..3`. | The runtime `0x18` descriptor being read by `sub_7FF7A7ECEB20` is not the raw on-disk `0x28` transform-track record. | CONFIRMED | Find the constructor that translates file-side records into runtime descriptors. |
| caller-supplied receiver object | In `sub_7FF7A7ECE940`, the fifth argument passed into `sub_7FF7A7ECEB20` is tied to the newly created loaded-node object. Combined with the direct `+0x168/+0x158/+0x148` calls later in `ECEB20`, this strongly suggests the packed path is feeding node-local transform setters during load-time construction. The newer `EC7DC0` evidence shows that at least one such setter is still an adapter rather than the deepest storage sink. | The decode consumer is part of load-time object construction and is likely a node-local rotation sink, but final class/type naming and the deepest concrete write are still open. | STRONGLY SUPPORTED | Resolve the receiver object's `+0x158` implementation. |
| selector-byte writer | The runtime table shape and lookup are now clearer, but the exact function that writes the `+0x10` mode/type byte has not yet been isolated. | This part of the bridge remains incomplete. | NOT YET KNOWN | Trace construction of the descriptor table at `owner+0xBC/0xC0` or the sibling lookup table at `base+172/176/184`. |
| final transform application | The current code path clearly decodes and forwards quaternion values during load-time object construction, and `+0x158` now has strong evidence as the rotation slot within a node-style transform interface. What is still missing is both the deepest concrete receiver implementation and proof tying that receiver to bones versus generic scene nodes. | It is now reasonable to call this a node/local-rotation sink, but still premature to elevate it to a fully named confirmed bone-writer or to a proven per-frame playback path. | STRONGLY SUPPORTED | Find the concrete implementation behind the deepest proven `+0x158` call and search separately for a time-advance/update caller. |

## Safe To Implement Now

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| runtime/body/eye asset reading | The dependency names, BRES headers, string regions, track arrays, and known texture formats are all stable enough to parse offline. | A read-only offline viewer can already implement: BUD extraction reuse, BRES header parsing, string extraction, runtime dependency listing, and DDS preview for known dragon body/eye textures. | STRONGLY SUPPORTED | Build a minimal loader that reports dependencies and previews textures before deeper mesh/anim work. |
| transform-track inspection | The `0x28` track records and their compact payload pointers generalize across two runtime dragons. | An offline inspector can safely enumerate channels and payload offsets now, even before every field is semantically decoded. | STRONGLY SUPPORTED | Generate per-track JSON dumps from more runtime dragons. |
| mesh-owner inspection | The mesh-owned stage-0/stage-1/skin-stage pointer chain is now proven across both runtime samples. | An offline viewer can safely expose mesh-owned subregion inspection, raw-shape classification, and DDS export without claiming final render correctness. | STRONGLY SUPPORTED | Add a UI layer over the current runtime inspector output. |
| flat sprite-mesh preview boundary | Crystal stage 0 now strongly supports a `516 x 28` interleaved vertex-like payload plus five primitive-group descriptors, and several primitive payloads already look like `3 x uint16` triangle lists. | A first orthographic flat textured sprite-mesh preview is becoming plausible, but still should not be called solved until the executable-side reader proves POSITION and texcoord lane meanings. | STRONGLY SUPPORTED | Prove the exact consumer of `0xC098` or one of the stage-0 primitive-group targets before rendering. |
| `viewer_prototype/runtime_inspector.py` | A local prototype can now safely expose `inspect-bres`, `inspect-mesh`, `inspect-dds`, and `compare-runtimes` over the solved header/root/track/DDS layers. | The offline viewer work can start immediately at the inspection/preview level without waiting for full skinning or animation playback. | CONFIRMED | Use this prototype output as the basis for a later UI layer. |
| `viewer_prototype/resolve_dml_body_type.py` | The new helper resolves a dragon by `ID` or mesh stem through `dragon_types.json`, joins `BODY_TYPE` into `dragon_body_type_specific_data.json`, and can optionally exact-search BUD/PAK roots for the resulting mesh and shared anim asset names. | The future viewer can use authoritative dragon metadata first, then exact archive lookup, instead of guessing shared animations from visual similarity or wiki labels. | CONFIRMED | Integrate this resolution chain into the viewer search UI and asset browser. |
| `viewer_prototype/probe_shared_animation_application.py` plus `viewer_prototype/tiki_tail_stride4_decoder.py` | The new probe layers shared-family resolution, exact node-name binding, and the Tiki adult tail stride-4 quaternion patch into one JSON output. The current output is saved under `generated/experimental/dragon_tiki_2024_runtime/shared_anim_probe/`. | A viewer/debugger can now safely expose "which shared anim applies to this dragon?" and "which tail local quaternions are currently known?" before a full animation player exists. | CONFIRMED | Add a debug pose exporter or a per-frame node-local JSON dump as the next viewer milestone. |
| `viewer_prototype/export_shared_anim_debug_pose.py` | The new exporter writes a machine-readable per-frame local transform subset for the resolved shared animation. For Tiki adult it emits `48` frames of local quaternion data for `Tail_bone01/02/03-node`, together with the authoritative assignment, exact binding summary, and unresolved shared-track inventory. | The project now has a concrete bridge from shared-animation assignment into viewer-ready node-local debug data, without overclaiming full playback. | CONFIRMED | Feed this export into a renderer-side pose overlay or skeleton debugger before expanding the decode surface. |
| `viewer_prototype/inspect_shared_anim_clip_table.py`, `dragon_gorillabody_anim.bdae`, and current DML IDB `0x7FF720952D00` | The shared Gorilla animation BDAE contains a named `0x18` clip/state table: `33` entries at `0xD20A0..0xD23B8` with shape `stringPtr, 0, start, end, 0, 0`. Examples include `idle_basic = 0..2000`, `idle_walk = 6667..8333`, `levelup = 40000..41833`, `attack = 60000..61333`, and `win = 80000..81967`. IDA proves `0x7FF720952D00` reads owner `+0x54/+0x58`, walks this `0x18` table, `strcmp`s the requested clip name, and returns the matching entry pointer. | Shared body-type animation assets are executable-consumed named clip bundles. This directly explains why a whole-BDAE/global-timeline GIF clamps or tweaks: the viewer must select a named state/subclip and then sample through the proper clip-time mapping. | CONFIRMED | Follow the returned entry pointer through `0x7FF721552F60` and downstream vmethods to connect selected clip time to the row-table evaluator. |
| current DML IDB `0x7FF720C99370` | The `SetNodeAnimation` route uses the same clip-table lookup, applies the selected clip/index through animation-object vmethods, then computes `([object+0x2C] - [object+0x28]) / durationArg` before calling another timing/speed vmethod. | Selected clip ranges become live animation-object state at `+0x28/+0x2C`; these are strong candidates for active clip start/end used by playback timing. | STRONGLY SUPPORTED | Resolve the concrete vmethod that writes `+0x28/+0x2C`, then connect those fields to the row-table sampler time argument. |
| `viewer_prototype/render_tiki_debug_skinning.py` plus handoff captures `tiki_auto_weights.bin`, `tiki_auto_24_skin_matrices.bin`, `tiki_all_local_trs_1780180013.txt`, and `tiki_missing_helper_nodes_1780180980.txt` | Tiki's current mesh vertex count is `437`, the weight blob parses cleanly as `437` repeated `20`-byte records of `4 x u8 slot indices + 4 x f32 weights` with a trailing zero footer, the matrix blob is exactly `24 x 4x4`, and the helper/local dumps are sufficient to render three captured-pose variants: direct captured-palette, helper-root `0x27190975200` round-trip, and a stronger Tiki-specific branch mix using `0x27190978200` for dominant slots `4/9`, `0x27190979400` for slot `5`, and `0x2719097aa00` for slot `10`. The renderer now also consumes descriptor-row local TRS frames, including mapped translations/scales when present; for the current Gorilla/Tiki clip, mapped translation/scale rows are essentially constant/rest-like, so visible diagnostic motion is rotation-dominated. | The viewer now has a real experimental debug skinning bridge for Tiki: not final playback yet, but no longer only static mesh plus disconnected quaternion exports. The current biggest remaining bug appears to be subtree/local-space palette composition and missing real clip/evaluator sinks, not bad mesh parsing. | STRONGLY SUPPORTED | Confirm the helper-root and branch-local corrections against executable-side skinning code, resolve virtual evaluator sinks, and replace normalized diagnostics with real clip-window playback. |
| runtime service-injection breakpoint | `0x7FF72092C7E0` explicitly clears `node + 328`, but runtime node creation now has a proven recovery path: `Scene_CreateObjectFromAssetName -> parent virtual +200 -> 0x92C390 -> 0x92C5C0 -> child virtual +496 (0x92EF20)`. That path copies the parent's non-null service into the new child subtree and notifies the service listeners. Mesh-specific consumers such as `0x92F060`, `0xA61220`, and `0xA61340` only make sense after this attach-propagation step has happened. | The decisive remaining bridge is no longer “where is `+328` injected somewhere?” but “which live scene parent first holds the service hub that is then propagated into the dragon subtree?” This is materially closer to the real engine skinning boundary than the earlier vague breakpoint model. | CONFIRMED for the propagation chain, STRONGLY SUPPORTED for the first-root-parent interpretation | Resume from parent attach callsites and the object behind `0x7FF720A2F750` / `Scene_CreateObjectFromAssetName` parent `+200` to identify the first already-serviced node in the dragon load path. |
| compact quaternion decoder research | `ED17B0`, `EDDA10`, `EDCC30`, and `EDD7B0` provide enough executable-side evidence to prototype decoder experiments on captured payload slices. | Quaternion decoding experiments are viable now, but still need one fully proven disk-to-runtime bridge for a named track. | STRONGLY SUPPORTED | Use one non-zero `extraRel` channel such as `Lwing_bone01-node-rotation` as the next bridge target. |
| first generalized playback milestone | Shared-family resolution, exact rig binding, ordinary shared-track file anchors, descriptor-row keyframe export, mode-3 timing scale, normalized/engine-ms diagnostics, concrete runtime decode-handler families, and a live-float evaluator path are already in place. The remaining blockers are the cooked `0x18` descriptor builder for ordinary channels, the clip/subclip time source, downstream virtual evaluator sinks, and final skinning-space integration. | A first broad debug playback of shared body-type motion is now close enough to keep patching the viewer experimentally, but full correct animated mesh playback still needs those bridges. | STRONGLY SUPPORTED | Resolve one ordinary shared-body channel end to end through `0x9F9CF0` and compare its sink output against our JSON sampler. |
| full static mesh rendering | The mesh-owned runtime-payload bridge is now real, but the exact vertex/index/UV/weight lane layouts have not yet been proven against executable-side readers. | A general static mesh renderer is still not safe to call solved. | NOT YET KNOWN | Identify the mesh reader that consumes the stage-0, stage-1, and skin-stage blocks. |

## IDA Playback Sink Update - 2026-06-12

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| `0x7FF72094FDAE-0x7FF72094FDC4` and `0x7FF72094FDCD` | The current IDB computes `r9 = owner + s32[owner+0xC0] + 0xC0 + index*0x18`, then reads selector byte `[r9+0x10]`. Selector `0/1/2/3` calls builder-interface slots `+0x168/+0x170/+0x178/+0x180`. | The cooked runtime descriptor table is now concretely shaped in the active IDB: owner-relative base, `0x18` stride, selector byte at `+0x10`, four builder routes. | CONFIRMED | Resolve the builder interface in `var_1F0` and tie one named track to a concrete evaluator-family vtable. |
| `0x7FF72094EBA8-0x7FF72094EBAC` | `sub_7FF72094EB20` stores `var_1F0 = arg0 + 8`; the selector dispatch later dereferences `[var_1F0]` and calls the selector-specific methods on that object's vtable. | The four selector routes belong to a helper/builder object owned by the first argument to the dispatcher at offset `+8`, not to a random global. | CONFIRMED | Trace callers to find where `arg0+8` is initialized and which concrete class supplies the builder methods. |
| `0x7FF72094E940` and `0x7FF720950BB0` | `0x94E940` uses the helper object at `a1+8` to create a node through virtual `+0x130`, calls `0x94EB20`, then finalizes the node. `0x950BB0`, called by `Scene_LoadNodeAssetAsync_Callback`, loops `0x60`-byte child entries and calls `0x94EB20` for each child before attaching/finalizing. | The descriptor dispatcher is firmly on the loaded-node construction and child assembly path; the helper at `a1+8` is both node factory and selector-builder provider. | CONFIRMED | Resolve the helper class behind `a1+8` and determine how its selector methods choose concrete evaluator families. |
| evaluator-family vtable blocks around `0x7FF722D333B0` | Raw qword scans show repeated `0xB0`-sized evaluator-family blocks. The corrected live vptr alignment is block `+0x18`: `+0x60/+0x68` are direct/blended output methods, `+0x70/+0x78` are time-sampling wrappers, and `+0x90/+0x98` are higher-level wrappers such as `0x7FF7209B4E80`/`0x7FF7209B4F60`. | The virtual calls made after key/alpha sampling now land on real output evaluator methods instead of the previously misaligned table slots. | CONFIRMED for raw qwords, STRONGLY SUPPORTED for vptr alignment | Find the constructor or runtime descriptor that stores this vptr into a cooked evaluator object. |
| `0x7FF72094CFA0` / `0x7FF72094CF20` | The direct method reconstructs a quaternion sample and calls receiver virtual `+0x158`; the blended method calls `0x7FF72095D7B0`, reconstructs two quaternions, blends them through `0x7FF720955D60`, then calls receiver `+0x158`. | The runtime animation evaluator has a confirmed local-rotation sink family. | CONFIRMED | Tie a named ordinary shared rotation track, preferably `head_ctrl-node-rotation`, to this evaluator family through the cooked descriptor object. |
| `0x7FF72094AE80` / `0x7FF72094AE00` and siblings | Direct/blended three-component value methods call receiver virtual `+0x168`. | The runtime animation evaluator has a confirmed local-translation sink family. | CONFIRMED | Tie `body_root-node-translation` to this evaluator family through the cooked descriptor object. |
| `0x7FF72094B090` / `0x7FF72094B010` and siblings | Direct/blended three-component value methods call receiver virtual `+0x148`. | The runtime animation evaluator has a confirmed local-scale sink family. | CONFIRMED | Tie one ordinary Gorilla scale track to this evaluator family through the cooked descriptor object. |
| playback readiness | Shared body-type assignment, exact Tiki node binding, row-table keyframe export, mode-3 timing, and IDA-proven local TRS sinks now line up. | A first viewer-side debug animation loop is now justified, but game-accurate playback still needs the cooked descriptor builder, clip/subclip time source, and final skinning-space composition. | STRONGLY SUPPORTED | Implement the viewer loop behind an experimental flag and keep tracing descriptor construction plus clip-state selection. |

## Runtime Descriptor Builder Milestone - 2026-06-13

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| current DML helper singleton `0x7FF722D333A0` | The static helper object is initialized through `0x7FF722717B50 -> 0x7FF7209AF990`, which writes vtable `off_7FF722D33068`. This vtable contains the node factory slot `+0x130` and selector builder slots `+0x168/+0x170/+0x178/+0x180`. | The BRES/node construction helper and the runtime descriptor selector helper are the same concrete singleton family. | CONFIRMED | Find the higher-level owner that retrieves this helper during dragon/shared-animation loading. |
| current DML `0x7FF72094FDAE-0x7FF72094FE7F` | The matched cooked descriptor pointer is computed as `owner + s32[owner+0xC0] + 0xC0 + index*0x18`; selector byte `+0x10` chooses one of four helper builder slots, and the descriptor pointer remains in `r9` as the fourth call argument. | The cooked descriptor is not only looked up by name; it is passed directly into the chosen runtime object constructor. | CONFIRMED | Recover the writer/populator for the owner `+0xBC/+0xC0` descriptor table. |
| current DML selector builders | Selector `0/1/2/3` builders allocate `0x200/0x208/0x210/0x228` bytes and call constructors `0x7FF720A00F40`, `0x7FF720A016A0`, `0x7FF720A00740`, and `0x7FF7209FFCF0`. | The selector byte represents four concrete parameterized runtime object families. | CONFIRMED | Determine which family ordinary shared rotations, compact rotations, translations, and scales select. |
| common constructor `0x7FF720A368D0` | Stores descriptor pointer at object `+424`, consumes descriptor `+0x00`, `+0x08`, and flag byte `+0x11`; selector constructors consume `descriptor+0x14` as a relative pointer to parameter payload. | The runtime `0x18` descriptor draft is now: name/binding at `+0x00`, secondary handle at `+0x08`, selector at `+0x10`, flag at `+0x11`, relative parameter payload at `+0x14`. | CONFIRMED for consumers, STRONGLY SUPPORTED for semantic labels | Match copied payload fields against file-side row/stride/base descriptors for one named track. |
| `0x7FF72092E9E0` and selector vtable `+0x210` method `0x7FF720A00360` | Descriptor `+0x08` is consumed as a C string and copied to selector object storage at `+56`. The post-build `+0x210` method uses the descriptor-derived flag at object `+432` to choose/update active pointer `+440`. | The cooked descriptor now has proven text identity plus a proven active-target/source selection flag. | CONFIRMED | Dump a populated descriptor table to compare `+0x08` text against known transform track names and discover the meaning of `+0x11`. |
| playback readiness implication | We now have data-driven body-type assignment, BRES shared-animation row decoding, mode-3 timing, local TRS sink families, and cooked descriptor consumption into runtime evaluator objects. | The project is close to a truthful debug playback layer. The remaining hard blockers are the cooked descriptor-table writer, clip/subclip state selection, and final skinning-space composition. | STRONGLY SUPPORTED | Continue from descriptor-table population or safe live table dumping, then validate one ordinary track end to end. |

## Current Descriptor-Construction Correction - 2026-06-13 Follow-Up

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| `0x7FF720A110A0` | Builds `qword_7FF722E9DE20` as 119 runtime field offsets by querying a freshly constructed runtime object. | `qword_7FF722E9DE20` is a dynamic field-offset table. It should not be confused with the case-12 cooked animation/transform descriptor table. | CONFIRMED | Keep report terminology explicit: "dynamic field-offset table" versus "case-12 0x18 descriptor table." |
| `0x7FF7209B7920` | Constructs a large runtime object from a source record, copies many scalar/vector/string fields through the dynamic field-offset table, and finalizes it into the runtime scene/resource graph. | This is a broad BRES/source-record-to-runtime-object cooker and a strong upstream lead, but not yet the exact cooked descriptor-table writer. | STRONGLY SUPPORTED | Follow the object stored at construction owner `+0x1A8` and compare source offsets with BRES record bytes from Tiki/Gorilla/Crystal/Metal samples. |
| `0x7FF7209B7CE0-0x7FF7209B7EFF` | Switches on source `+0x40`, not runtime descriptor `+0x10`, and copies relative-payload floats into runtime fields. | This is a separate source-value format switch. It should not be treated as the animation selector byte. | CONFIRMED | Classify the source record type that owns fields `+0x40/+0x44`. |
| `0x7FF72094FCDF-0x7FF72094FDC4` | The known descriptor consumer reads the case-12 table with BRES-style relative addressing and `0x18` stride. | The table might be relocated/cooked file data rather than heap-populated entries; searching only for stores to `descriptor+0x10` may miss it. | STRONGLY SUPPORTED | Add a file-side pattern search for `count + rel + 0x18 entries + known names`, then compare candidates to the executable layout. |

## Static Viewer Milestone

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| Crystal runtime `stage0 -> 0xC098` | The generic renderer reconstructs Crystal from `516` candidate records of `28` bytes each, chooses `XZ` as the best projection, and produces a coherent body-textured Crystal Lady silhouette with the confirmed DDS body atlas. Validation checks now prove: exact payload size match, all adjusted indices in bounds, claimed primitive ranges matching adjusted ranges, and full `0..515` vertex coverage. | Crystal stage-0 geometry is now `VISUALLY VALIDATED / FILE-STRUCTURALLY VALIDATED` for static display and export. | CONFIRMED | Keep using Crystal as the baseline regression sample while resolving eye/effect/shadow pass assignment and skin/bind data. |
| Metal runtime `stage0 -> 0x120DD8` | The same generic renderer reconstructs Metal from `529` candidate records of `24` bytes each, chooses `XZ` again as the best projection, and produces a coherent body-textured Metal Seahorse silhouette. Validation checks likewise pass: exact payload size match, all adjusted indices in bounds, claimed primitive ranges matching adjusted ranges, and full `0..528` vertex coverage. | Crystal was not a one-off. The runtime-BRES mesh-owner/stage/primitive reconstruction model generalizes across at least two dragons. | CONFIRMED | Add a third dragon when useful, but the static body-mesh path is now strong enough to serve as a real viewer/exporter foundation. |
| Crystal vs Metal candidate stream layouts | Crystal uses a `516 x 28` candidate stream with `float3 + marker + float2 + marker`; Metal uses a `529 x 24` candidate stream with `float3 + float2 + marker`. Both preserve coherent textured silhouettes. | The static runtime viewer must detect per-runtime candidate layout from stage descriptors rather than hardcoding one global vertex format. | CONFIRMED | Tie both layouts to the executable-side mesh reader and identify the meaning of the preserved marker/unknown words. |
| per-role render attempts | Body DDS renders coherently for both runtimes. Crystal eye rendering isolates `group_3` as a strong eye-pass candidate; Metal keeps `group_3` as an unresolved alternate pass because both body and eye centroid alpha stay at `0.0`. Crystal still misses `fx_spark_alpha.tga` and `shadow_dragon.tga`; Metal still misses `shadow_dragon.tga`. | The project has crossed into a working experimental DML static sprite-mesh viewer/exporter, while material/pass assignment remains the next render-layer problem. | STRONGLY SUPPORTED | Recover record-backed body/eye/effect/shadow bindings from BRES or IDA instead of relying only on visual-fit evidence. |
| exporter outputs in `generated/experimental/*/render_runtime_mesh/` | Both runtimes now emit body/eye/group-debug PNGs, transparent PNGs, UV overlays, OBJ, glTF, JSON summaries, and missing-dependency reports. | Experimental OBJ/glTF export is justified now for solved static geometry layers. | CONFIRMED | Preserve these assets as regression fixtures and keep validation warnings in the renderer so format regressions surface immediately. |

## GUI Integration Milestone

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| `viewer_prototype/dml_asset_viewer_gui.py` | New workspace GUI imports the V6 BUD/loose-asset indexer, exposes asset search, dragon search, export, selected-BDAE preview, selected-dragon preview, optional shared-body-animation lookup, and an in-app clean PNG preview. | The project now has a proper viewer-facing browser shell without modifying the original Downloads V6 script. | CONFIRMED | Launch `RUN_DML_ASSET_VIEWER_GUI.bat`, auto-index DML roots, select Tiki 2024, and preview the selected dragon. |
| `RUN_DML_ASSET_VIEWER_GUI.bat` | Double-click launcher points at the bundled Codex Python runtime and starts the GUI from the workspace. | The tool can be used without typing the long Python command. | CONFIRMED | Double-click the launcher and verify the GUI starts on the user's desktop. |
| `generated/gui_sessions/smoke_tiki_static/` | Smoke test rendered `dragon_tiki_2024.bdae` through the safe session wrapper. Output summary reports `D_Tiki_2024-mesh`, `437 x 24` vertices, two primitive groups covering `0..436`, all adjusted indices in bounds, and clean/cropped XZ preview PNGs. | The GUI backend is wired to the stable static renderer and avoids the currently deformed experimental skin path by default. | CONFIRMED | Use the same session wrapper from the GUI and compare against the Tiki reference portrait. |
| GUI `Experimental skinning` checkbox | The GUI leaves skinned mesh playback disabled unless explicitly checked. | This prevents the current palette-space deformation from polluting ordinary asset browsing or static preview work. | CONFIRMED | Keep this opt-in until the executable-side palette composition is reproduced offline. |

Safe to implement now in the GUI: asset index/search/export, dragon lookup through shipped metadata, static runtime BDAE preview, body texture render, OBJ/glTF export, missing dependency reports, and diagnostic skeleton overlays.

Not safe as default yet: final material pass assignment, GPU-equivalent skin deformation, game-equivalent clip timing, particle simulation, and edited BRES writing.

## FX / Animated Texture Milestone

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| `dragon_paper_lantern.bdae` string categories | Inspector now separates `fxTextureRefs = fx_ancient_attack_rings_01.tga, fx_paper_lantern.tga, fx_spark.tga`, `animatedTextureStrings = TextureAnim.bdae, textureOffset, TextureAnim-fx-profile_GLES2/CurrentTechnique`, and additive/premultiplied material/profile strings. | Some dragon FX are embedded material/animated-texture passes inside the main BDAE, not always separate `fx_*.bdae` files. | STRONGLY SUPPORTED | Recover the material/pass records that bind each FX texture and `textureOffset` data. |
| `fx_buble_dragon_buble.bdae` string categories | Tiny BRES has no mesh bridge and contains `fx_soap_bubble.tga` plus `node-node-gnps_emitter`. | Standalone FX BDAEs can be emitter/particle graphs rather than skinned mesh assets. | STRONGLY SUPPORTED | Trace emitter record layout and engine particle node creation. |
| IDA strings | Read-only search found `ParticleSystem`, `_particle_custom_vx_attribute`, a particle texture-coordinate warning, and `node_copy-node-gnps_emitter` referenced from `0x7FF72115A800`. | The engine has a real particle/emitter path with texture-coordinate linkage. | STRONGLY SUPPORTED | Decompile `0x7FF72115A800` and related particle-system functions read-only. |
| `viewer_prototype/render_fx_texture_preview.py` | New helper emits raw RGBA, alpha visualization, straight-alpha, premultiplied-alpha, and additive preview outputs for FX atlases. | Viewer can safely preview FX atlas compositing families now, without claiming UV animation or particle simulation is solved. | CONFIRMED | Use on more `fx_*.tga/.dds/.png` files and compare with in-game FX. |

Safe to implement now: decode DDS-backed FX atlases, emit additive/premultiplied/alpha debug previews, and list animated-texture or particle/emitter evidence from BRES strings.

Not solved yet: `TextureAnim` UV scrolling, flipbook timing, particle spawning, over-life curves, exact FX/shadow/eye material assignment, and full skinned animation playback.
