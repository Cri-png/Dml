# Bind Pose Vs Playback

## Proven Load-Time Transform Construction

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| `0x7FF7A7ECE940` | Decompile shows: create a node-like object through a virtual method at `+0x130`, call `sub_7FF7A7ECEB20(..., &node_ptr)`, then call the created node's `+0xC8` method and `Scene_FinalizeLoadedNode`. | The currently traced transform/decode path is entered during loaded-node construction and finalization. | CONFIRMED | Identify the created object class and its concrete transform interface. |
| `0x7FF7A7ED0BB0` | Decompile walks child-like records in `96`-byte steps and recursively calls `sub_7FF7A7ECEB20(...)` for each one before attaching them. | The surrounding caller graph is a hierarchy-construction path, not an obvious per-frame update loop. | STRONGLY SUPPORTED | Map the `96`-byte child record family back to file-side owners. |
| `0x7FF7A7ED005B-0x7FF7A7ED00AF` inside `sub_7FF7A7ECEB20` | The same runtime receiver object is called directly with `+0x168` using a vec3-like value, then `+0x158` using a quaternion, then `+0x148` using a second vec3-like value. | The proven inlined path looks like direct local translation / rotation / scale application while a node is being built. | STRONGLY SUPPORTED | Resolve the concrete receiver class and its storage fields. |

## Decode Helper Fan-In And Consumer Shape

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| `0x7FF7A7ED17B0` xrefs | Code xref query returns exactly one code caller: `0x7FF7A7EDDA10`. | The compact quaternion unpacker is a leaf decoder, not a broad update-loop hub. | CONFIRMED | Keep tracing the wrappers that feed `EDDA10`. |
| `0x7FF7A7EDCC30` xrefs | Code xref query returns exactly two code callers: `0x7FF7A7ECD110` and trampoline `0x7FF7A7ED86F0`. | This packed-decode family is also reached through narrow interface wrappers. | CONFIRMED | Compare the consuming versus non-consuming wrapper variants. |
| `0x7FF7A7EDD7B0` xrefs | Code xref query returns exactly two code callers: `0x7FF7A7ECCF20` and trampoline `0x7FF7A7ED8680`. | The third packed-rotation family has the same wrapper-heavy shape. | CONFIRMED | Tie each wrapper family to a selector value once the builder is found. |
| `0x7FF7A7ECD090` and `0x7FF7A7EC7DC0` xrefs | Code xref queries return zero direct code callers for both; only data/vtable xrefs were found. | These consumer-facing methods are primarily reached through virtual dispatch, which makes them look like interface implementations rather than a standalone tick function. | CONFIRMED | Recover the owning vtable/class if a concrete type is needed. |
| `0x7FF7A7ECCBA0`, `0x7FF7A7EDC810`, `0x7FF7A7EDCA20`, `0x7FF7A7EDD2B0`, `0x7FF7A7EDDAF0` | Each helper decompiles to "decode/interpolate packed values into a temporary quaternion-like result, then pass that result to `sub_7FF7A7ED5D60` and/or a `+0x158` consumer", and each currently has only one code caller plus data/vtable xrefs. | The currently visible helper family still looks like a loader-side transform adapter stack, not a clearly identified runtime playback loop. | STRONGLY SUPPORTED | Look for a separate time-advance function that calls the same helpers repeatedly on already-created nodes. |
| `0x7FF7A7EC7DC0 -> 0x7FF7A7ED2090 -> receiver+0x158` | `EC7DC0` initializes an identity quaternion, calls `ED2090(a2, a3, a4, &tmp)`, then forwards `&tmp` to another object's `+0x158`. `ED2090` itself accumulates quaternion/axis-angle-like transforms across `a3` entries. | One apparent `+0x158` implementation is still an adapter layer that computes or accumulates a quaternion and then forwards to a deeper `+0x158` sink. | CONFIRMED | Follow the second receiver object to find the concrete final transform sink. |

## New Playback-Style Sampler Evidence In Current DML IDB

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| current DML `0x7FF7209B53E0` | This virtual slot calls `0x7FF7209B51D0`, passing the current float parameter `a4`, then branches on the returned mask/interp state and invokes object-specific virtuals at `+0x80` or `+0x88`. | This is not a pure load-time constructor helper; it has the shape of a runtime sampler/evaluator that chooses one sample or blends two samples based on current time. | STRONGLY SUPPORTED | Find a concrete caller that supplies advancing time rather than a one-shot construction parameter. |
| current DML `0x7FF7209B54B0` | This sibling slot calls `0x7FF7209B5030` and likewise switches between object virtuals at `+0x80` and `+0x88` depending on whether interpolation is needed. | There are at least two playback-style sampler entrypoints sharing the same generic time/interpolation model. | STRONGLY SUPPORTED | Determine which track-property families use `0x9B53E0` versus `0x9B54B0`. |
| current DML `0x7FF7209B42D0 / 0x45A0 / 0x4870 / 0x4B40` | These helpers compute interpolation factor `t` from two key-time values read as `u8`, `i16`, `u16`, or `i32`-like samples, scaled by `33.333332`, and can advance the key cursor when `t == 1.0`. | The engine has a generic key-time interpolation layer spanning multiple compact time encodings, which is a hallmark of real playback sampling rather than simple bind-pose initialization. | CONFIRMED | Tie one ordinary shared track's descriptor to one of these time formats. |
| current DML `0x7FF7209B4410 / 0x46E0 / 0x49B0 / 0x4C60` | These sibling helpers perform the same interpolation-factor logic but in the `0x9B51D0` branch that carries an extra selector/flags argument. | The playback-style sampler layer has paired variants, likely for related but not identical runtime property families. | STRONGLY SUPPORTED | Recover the meaning of the extra selector argument from the cooked runtime descriptor builder. |
| current DML handler families at `0x7FF722D332B8`, `0x7FF722D33418`, `0x7FF722D334C8`, `0x7FF722D336D8`, `0x7FF722D33788` | For five concrete rotation-handler class families, selector `3` maps to `vtable + 0x180 -> 0x7FF7209B53E0`, while selectors `0/1/2` route to direct decode and decode-plus-sink variants. | The cooked descriptor selector appears to distinguish between immediate decode/apply paths and a playback-capable sampler path. | STRONGLY SUPPORTED | Recover the selector byte for one ordinary shared-body track and test whether animated channels prefer selector `3`. |
| current DML `0x7FF7209F9450`, `0x7FF7209F9CF0`, `0x7FF7209FAE30`, `0x7FF7209FB5C0` | These higher-level routines all take a live `float` parameter, call into `0x7FF7209B51D0`, and then continue through wider per-property/per-output logic with copies, caches, or multi-entry loops. `0x7FF7209BCCF0` is a sibling evaluator that uses `0x7FF7209B5030`. | The sampler layer is already embedded inside broader runtime evaluation code, which means the project is past pure decoder research and into real playback-system reconstruction. | STRONGLY SUPPORTED | Identify the owning animation object/class for one of these evaluators and recover the caller that advances the float time parameter frame to frame. |

## Current Playback Status

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| current traced path overall | Every currently proven route runs through node creation, hierarchy construction, local-transform setter calls, or vtable-only decode adapters. | The known quaternion path is proven for load-time transform construction, but not yet for per-frame playback. | CONFIRMED | Keep searching for delta-time, frame-advance, or sampling-loop callers that reuse the same helpers. |
| current DML sampler layer overall | The `0x9B5030/0x9B51D0` plus `0x9B42D0..0x9B4C60` family now proves that at least one class family performs genuine key-time interpolation and sample-cursor advancement. | The project is no longer limited to load-time-only evidence; there is now solid executable-side evidence for a playback-style sampling layer, even though the top-level update/tick caller is still missing. | CONFIRMED | Find the caller that repeatedly supplies changing `a4` time values into this sampler layer. |
| current generalized playback readiness | Shared animation family resolution, exact Tiki rig binding, one tail-family local-quaternion export, ordinary file-side descriptor anchors, concrete runtime rotation-family handlers, and a real key-time interpolation/sampler layer are all now proven. The two still-missing bridges are the cooked `0x18` descriptor-table builder and the higher-level update/tick caller that advances time across ordinary channels. | The project is close to first broad debug playback of shared body-type channels, but not yet at correct full animated mesh playback. | STRONGLY SUPPORTED | Recover one ordinary channel end to end, then feed its sampled TRS into the existing static renderer before tackling final skinning/playback integration. |
| bind-pose versus authored initial transform | The file-side data is still track-like and selector-driven, but the code-side application point is load-time object construction. | It is not yet safe to say "bind pose only" or "reused for playback"; the correct status is that playback reuse remains unresolved. | STRONGLY SUPPORTED | Find a true update/tick caller or prove the absence of one in the nearby animation system. |
| missing update-time evidence | This pass did not recover a caller graph showing current-time advancement, frame/key selection, interpolation over already-created nodes, or a tick/update entry that applies the same quaternion sink repeatedly. | The per-frame animation evaluator is still a separate missing stage in the pipeline. | NOT YET KNOWN | Search outward from animation-system entrypoints rather than only inward from the decode helpers. |
