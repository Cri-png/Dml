# IDA BRES Pipeline - Initial Read-Only Evidence

## Session Status

| Address or path | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| IDA MCP session | `list_instances` returned active reachable instance `DragonManiaLegends.exe` on `127.0.0.1:13337` with IDB `C:\Users\crist\Downloads\DragonManiaLegends.exe.i64`. `server_health` returned `status=ok`, `imagebase=0x7ff7a7cb0000`, `auto_analysis_ready=true`, `hexrays_ready=true`. | The current database is usable for read-only reverse engineering right now. | CONFIRMED | Continue with MCP-only decompile/xref/disassembly queries. |
| IDA modification status | No renames, type changes, comments, patches, or saved-IDB edits were applied in this pass. | This report is purely observational and safe to verify against the current database. | CONFIRMED | Keep later proposed names/types separate until permission is granted. |

## Function Evidence

| Address | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| `0x7FF7A7ECEB20` | `analyze_batch` shows function size `0x170C`, prototype `_QWORD *__fastcall(__int64 *, _QWORD *, __int64, __int64, _QWORD *)`, callers at `0x7FF7A7ECE940` and `0x7FF7A7ED0BB0`, and many internal callees. Targeted instruction query confirmed `0x7FF7A7ECFDCD: movzx ecx, byte ptr [r9+10h]`, followed by `test ecx, ecx`, `jz`, `sub ecx, 1`, `jz`, `sub ecx, 1`, `jz`, `cmp ecx, 1`, `jnz`. The same local block starts with `lea r9, [rax+r9*8]` and `add r9, r13`. | This is a large dispatcher/constructor-like routine that uses a one-byte selector at runtime offset `+0x10` in a structure reached through `r9`. The observed branch ladder proves at least four selector cases in this block. It does **not** yet prove that `r9` is a raw on-disk BRES record. | STRONGLY SUPPORTED | Trace how `r9` is formed earlier in the function and identify which selector case(s) eventually feed the quaternion path. |
| `0x7FF7A7ED86C0` | Small `0x21`-byte function decompiles to `return sub_7FF7A7EDDA10(a2, a3, a4, a4, a6);` and assembly ends with `0x7FF7A7ED86DC: jmp sub_7FF7A7EDDA10`. | This is a trampoline/wrapper whose only purpose is to repackage arguments and tail-jump into `sub_7FF7A7EDDA10`. It independently confirms the previously observed `0x7FF7A7ED86DC` jump site. | CONFIRMED | Enumerate neighboring trampolines around `0x7FF7A7ED86C0` to see whether several adjacent selector cases share the same decoder. |
| `0x7FF7A7EDDA10` | Prototype `__int64 __fastcall(__int64 *, unsigned int, unsigned int, float, __int64)`. Decompile reads `v6 = *a1`, then uses `*(int *)(v6 + 36)` and `*(int *)(v6 + 44)` to build optional pointers from relative offsets. It then calls `sub_7FF7A7ED17B0(...)`. Xrefs-to include `0x7FF7A7ECD0DF` and `0x7FF7A7ED86DC`. | This function is a wrapper/adapter around a packed-data decoder. The raw evidence supports the earlier claim that offsets `+36` and `+44` inside a descriptor object participate in setting up the decode inputs. The current evidence still does not prove the descriptor is a raw BRES file record rather than a relocated/runtime object. | CONFIRMED | Walk callers backward and identify where the descriptor object at `*a1` is allocated/populated from file data. |
| `0x7FF7A7ECD090` | Prototype `__int64 __fastcall(__int64, __int64 *, unsigned int, unsigned int, float, __int64)`. Decompile loads constant `xmmword_7FF7A9D50DE0` into a stack buffer, calls `sub_7FF7A7EDDA10(a2, a3, a4, a5, &si128)`, then performs a virtual call through `(*(_QWORD *)a6 + 344)` / `[vtable+0x158]` with that buffer. | This proves one immediate consumer path for the decoded output: the wrapper decodes into a temporary buffer and forwards it to an object method. That strongly suggests evaluation/application plumbing rather than raw file parsing. | STRONGLY SUPPORTED | Identify the class behind `a6` and the meaning of the virtual method at vtable offset `0x158`. |
| `0x7FF7A7ED17B0` | Prototype `__int64 __fastcall(float **, int, int, __int64)`. Decompile reads lookup/base tables from two pointers inside `*a1`, reconstructs float components from packed bytes/16-bit values, calls `sqrtf`, and writes two consecutive decoded outputs. Xrefs-to only show caller `0x7FF7A7EDDA10`. | This is a compact quaternion-like decoder, and the `sqrtf(1 - x*x - y*y - z*z)` style recovery of the fourth component remains the strongest executable-side evidence gathered so far. | CONFIRMED | Map the table/payload pointers back to the owning descriptor and then to a specific named BRES rotation track. |

## Direct Xref Chain Observed So Far

| Address | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| `0x7FF7A7ED17B0` | `xrefs_to` returned one code caller: `0x7FF7A7EDDAB4` inside `sub_7FF7A7EDDA10`, plus one data xref. | The packed quaternion decoder currently has one directly observed executable wrapper in this path. | CONFIRMED | Work backward from `sub_7FF7A7EDDA10` rather than searching for more direct callers of `sub_7FF7A7ED17B0`. |
| `0x7FF7A7EDDA10` | `xrefs_to` returned code xrefs from `0x7FF7A7ECD0DF` and `0x7FF7A7ED86DC`, plus one data xref. | At least two code paths feed the same decode wrapper: one full caller and one trampoline. | CONFIRMED | Inspect other nearby wrappers/trampolines to see whether multiple format cases converge here. |
| `0x7FF7A7ECEB20` | `xrefs_to` returned code xrefs from `0x7FF7A7ECE9CF` and `0x7FF7A7ED0CAA`, plus one internal self-xref and one data xref. | `sub_7FF7A7ECEB20` is reused by nearby parser/dispatcher logic rather than being a dead isolated helper. | STRONGLY SUPPORTED | Decompile callers `sub_7FF7A7ECE940` and `sub_7FF7A7ED0BB0` after the file-side comparison pass. |

## Current Proven Statements

| Address or topic | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| Quaternion decode chain | Verified executable-side path: `sub_7FF7A7ECD090 -> sub_7FF7A7EDDA10 -> sub_7FF7A7ED17B0`, with `sub_7FF7A7ECD090` forwarding the decode result to a virtual method. | The compact-rotation path is proven as executable behavior, but not yet tied back to a specific on-disk BRES track record. | CONFIRMED | Backtrack the descriptor object and match it to a named runtime track. |
| Selector byte | Verified instruction `0x7FF7A7ECFDCD: movzx ecx, byte ptr [r9+10h]` and immediate branch ladder in `sub_7FF7A7ECEB20`. | A one-byte runtime selector governs a local case dispatch. The exact semantic meaning of the selector is still unknown. | CONFIRMED | Trace the selector value from object creation/population to the eventual decode case. |
| Raw BRES header loader | No function in this pass was proven to be the code that first recognizes `BRES` or relocates the file. | The current evidence is downstream of parsing and/or object construction, not yet the top-level raw loader. | NOT YET KNOWN | Trace from `"BDAE"` and file-open/load paths toward the first code that reads `BRES` bytes directly. |
| On-disk record mapping | None of the three decompilations alone proves how a specific named track such as `Lwing_bone01-node-rotation` maps to the runtime descriptor reaching `sub_7FF7A7EDDA10`. | The central bridge from file record to runtime selector/decoder remains open. | NOT YET KNOWN | Compare runtime file sections directly, then trace descriptor arrays and name-binding code in IDA. |

