# Mesh / Skin Bridge

## Proven Mesh Ownership

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| Crystal Lady mesh owner `0x8728` (`34600`), `-11312` relative to `data_start=0xB358` | String-backed fields at the start resolve to `Simple`, `dragon_crystal_lady002-mesh`, `dragon_crystal_lady002`, `shadow-mesh`, `shadow`. The same record stores `stage0Abs=0xBD10`, `stage0Size=904`; `stage1Abs=0x10808`, `stage1Size=328`; `skinStageAbs=0x109C0`, `skinStageSize=4360`. | This is a concrete runtime-BRES mesh owner record, not a loose late-file coincidence. | CONFIRMED | Find the executable-side reader for this owner family. |
| Metal Seahorse mesh owner `0x11DA18` (`1169944`), `+1127016` relative to `data_start=0xA7B0` | The same field pattern appears with `METAL_SEAHORSE_-mesh`, `METAL_SEAHORSE_`, `shadow-mesh`, `shadow`, plus `stage0Abs=0x120B08`, `stage1Abs=0x1250A8`, `skinStageAbs=0x125260`. | The high-level mesh-owner record layout generalizes across both runtime dragons. | CONFIRMED | Compare a third runtime dragon to test whether this owner layout is universal. |
| Crystal mesh-skin subrecord `0x87A8`; Metal mesh-skin subrecord `0x11DA98` | The `...-mesh-skin` owner sits immediately beside the mesh owner and participates in the same structured region. | Mesh and skin ownership are tightly coupled in one record family. | STRONGLY SUPPORTED | Prove whether the executable instantiates separate mesh and skin runtime objects from sibling records here. |
| Crystal `3DMesh_Skinned` owners `0x8BC0..0xBA78`; Metal `0x11DEB8..0x120470` | Repeated profile-like owners contain `Dragon`, `3DMesh_Skinned`, `3DMesh`, `Dragon_STENCIL`, `AdditivePreMul_Skinned`, and related material/profile names. | These look like render-profile descriptors associated with the mesh family, not the deep payload owner itself. | CONFIRMED | Tie one such profile descriptor to the scene/mesh creation path in IDA. |

## Crystal Mesh Owner Detailed Dump

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| Crystal owner `0x8728 + 0x40/+0x48` | `stage0Abs=0xBD10` (`48400`, `+2488` relative to `data_start`), `stage0Size=904`. | Stage 0 is a first-level payload descriptor block owned directly by the mesh record. | CONFIRMED | Match the stage-0 descriptor layout to an executable-side reader. |
| Crystal owner `0x8728 + 0x60/+0x68` | `stage1Abs=0x10808` (`67592`, `+21680`), `stage1Size=328`. | Stage 1 is a second structured payload block owned by the same mesh record. | CONFIRMED | Determine whether it controls attribute declarations, submesh ranges, or another mesh-side table. |
| Crystal owner `0x8728 + 0x98/+0xA0` | `skinStageAbs=0x109C0` (`68032`, `+22120`), `skinStageSize=4360`. | Skin-owned payload is explicitly attached at the same owner level as stage 0 and stage 1. | CONFIRMED | Tie it to executable-side skin-weight or binding reads. |
| Crystal stage 0 block `0xBD10` | First words are `600, 504, 408, 312, 216`, then `1`, then a run of `(4,108) (4,116) (4,124) (4,132) (4,140)`, then the first nested target pointer at block-relative `+0x60`. | Stage 0 is a structured descriptor block with ordered counts/offset-like values, not a flat numeric arena. | CONFIRMED | Recover the meaning of the repeated `(4, offset)` pairs from code. |
| Crystal stage 0 nested target `0xC098` (`+3392` from `data_start`) | The first 24 dwords show a repeating `vec3-like, 0xFFFFFFFF, vec2-like, 0xFFFFFFFF` cadence: `(-22.200249, 3.885846, -26.788742, FFFFFFFF, 0.037246, 0.594966, FFFFFFFF, ...)`. | Strongest current raw-shape candidate for interleaved per-vertex-like attribute groups. | STRONGLY SUPPORTED | Prove stride and lane meanings in code before calling them positions/UVs. |
| Crystal stage 0 nested targets `0xF908`, `0x10088`, `0x104F8`, `0x10738`, `0x10748` | These regions are dominated by low `u16` and `u32` values. Typical starts are `0,1,2,2,3,0,3,4,5,5,0,3...` and similar short monotonic triangle-like runs. | Strongest current raw-shape candidate for topology/index-style tables. | STRONGLY SUPPORTED | Tie one table to an executable triangle unpack or draw-primitive builder. |
| Crystal stage 1 nested target `0x10950` (`+22008`) | Repeating 24-byte mixed-float groups begin `(-28.029484, -18.54188, 54.80867, 0, 1, 0xFF000000, 22.99478, -18.54188, 54.80867, 1, 1, 0xFF000000, ...)`. | Float-rich descriptor or lane payload with stable group size, but not yet semantically named. | STRONGLY SUPPORTED | Find a reader that treats this region with a fixed 24-byte stride. |
| Crystal stage 1 nested target `0x109B0` (`+22104`) | Starts `65536, 131074, 3, 0, 3168, 0, 3152, 0, 3136, 0, 3120, ...`. | Small-integer-rich / pointer-table-like region, likely companion metadata rather than direct float attributes. | STRONGLY SUPPORTED | Compare the descending offsets against stage or submesh counts in code. |
| Crystal skin-stage nested target `0x11AC8` (`+26480`) | Repeating groups include integer-like IDs plus float pairs summing close to 1.0: `(4627, 0.854918, 0.145082, 0, 0, 19, 1.0, 0, ...)`, later `(0.841372, 0.158628)`, `(0.675761, 0.324239)`. | Strongest current raw-shape candidate for normalized skin weights / influence records. | STRONGLY SUPPORTED | Prove the exact weight lane layout and accompanying ID fields in code. |
| Crystal skin-stage nested target `0x14318` (`+36800`) | Mixed small integers plus direct string-region references: `8468 -> eye_crystal_lady.tga`, `10396 -> Dragon_center-node`; nearby `6076` falls just before `strings_start` inside a non-string metadata area that also contains the literal `0,0,0,930`. | Binding / lookup style table, not just raw weights. It mixes IDs, string refs, and additional small offsets. | STRONGLY SUPPORTED | Correlate referenced node/texture names with binding functions and scene-node assembly code. |

## Metal Cross-Check

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| Metal stage 0 primary target `0x120DD8` | Also float-rich, but with a slightly different cadence: `(-22.372122, 3.816076, -27.454962, 0.934970, 0.193538, FFFFFFFF, ...)`. | The float-rich mesh-owned target generalizes, but the exact packing is not identical byte-for-byte across dragons. | STRONGLY SUPPORTED | Determine whether one runtime uses a different lane order or an additional scalar before the sentinel. |
| Metal later stage 0 targets `0x123F70`, `0x124CF0`, `0x124D00`, `0x124FE8` | Dominated by low-width integer runs, matching Crystal’s stage-0 secondary-target shape class. | The index/topology-like evidence is stable across both runtime samples. | STRONGLY SUPPORTED | Match one of these ranges to triangle unpack helpers in code. |
| Metal skin-stage nested targets `0x126130`, `0x128A88` | Primary target shows repeated weight-like float pairs summing near `1.0`. Secondary target is small-integer-rich and includes a direct string ref `9828 -> Bone10`. | Metal reproduces the Crystal split between weight-like primary skin data and binding-like secondary metadata. | STRONGLY SUPPORTED | Prove whether the binding table indexes bones, nodes, or skin slots. |

## Executable-Side Reader Status

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| `0x7FF7A821C140 -> 0x7FF7A7F0FC70 -> 0x7FF7A7F55720` | This branch reaches the strings `CStaticBatchBaker was not able to bake primivite.`, `CStaticBatchBaker was not able to bake vertex.`, and then selects index unpack helpers by `*(WORD *)(a4 + 24) == 1` or `2`. | This is a real mesh/index build path with primitive unpacking, but it is not yet tied directly to the Crystal `stage0/stage1/skinStage` owner family. | STRONGLY SUPPORTED | Find the parent that feeds one of the Crystal stage-owned integer tables into this path. |
| `0x7FF7A99822B0 -> 0x7FF7A7F66130` and `0x7FF7A7F653E0` | These functions process attribute/property semantics, including `texcoord`, `transform`, and sibling helpers that reference `skin_weights`, `skin_indices`, `POSITION`, and semantic tables with 24-byte stride. | This is a strong candidate mesh attribute-linking path, again not yet directly tied to the runtime-BRES stage blocks. | STRONGLY SUPPORTED | Prove that a stage-owned float-rich target becomes one of the input arrays handled here. |
| current executable gap | No read-only trace yet shows a function loading `0x8728` and then reading `+0x40`, `+0x60`, and `+0x98` as the Crystal mesh-owner stage pointers. | The exact runtime-BRES mesh-owner reader is still missing. | NOT YET KNOWN | Search for code that reads three sibling pointers/sizes at these owner-relative offsets or resolves a `...-mesh` named record into a mesh scene object. |

## Crystal Stage 0 Descriptor Pattern

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| Crystal stage 0 header `0xBD10 + 0x2C = 516` | The first external target begins at `0xC098`; the next target begins at `0xF908`; `0xF908 - 0xC098 = 14448`, and `14448 / 516 = 28` exactly. | The first stage-0 external payload is a `516 x 28` interleaved record array. That is a much stronger vertex-stream-like result than a generic “float-rich blob”. | STRONGLY SUPPORTED | Prove in code that the first three floats are POSITION-like and the next two floats are texture-coordinate-like. |
| Crystal stage 0 descriptor starts at block-relative `216, 312, 408, 504, 600` | These five internal offsets are stored directly in the stage-0 header and each lands on a 96-byte internal block. | Stage 0 contains five explicit primitive-group descriptors after the vertex-like payload header, not just one undifferentiated arena. | CONFIRMED | Match one 96-byte descriptor layout to the executable-side primitive builder. |
| Descriptor at `0xBD10 + 216` | Internal block contains `typeCode=4`, `triangleLikeCount=320`, `u16Count=960`, `maxIndexLike=243`, target `0xF908`, size `1920`. `1920 / 2 = 960` and `960 / 3 = 320`. | Strong file-side candidate for a `3 x uint16` triangle-index stream over the first `244` vertices. | STRONGLY SUPPORTED | Prove this exact target reaches the `index type 1` primitive-unpack path. |
| Descriptor at `0xBD10 + 312` | Internal block points at `0x10088`, size `1136`, with `triangleLikeCount=189`, `u16Count=567`, `minIndexLike=244`, `maxIndexLike=405`. The payload size leaves one extra `u16` beyond the 567 meaningful values. | Strong file-side candidate for a second triangle-index stream plus one trailing pad/marker word. | STRONGLY SUPPORTED | Determine whether the extra `u16` is padding, restart, or another small control field. |
| Descriptor at `0xBD10 + 408` | Internal block points at `0x104F8`, size `576`, with `triangleLikeCount=96`, `u16Count=288`, `minIndexLike=406`, `maxIndexLike=486`. | Strong file-side candidate for a third `3 x uint16` triangle-index stream. | STRONGLY SUPPORTED | Tie one `406..486` range to a submesh or piece/material split. |
| Descriptor at `0xBD10 + 504` | Internal block points at `0x10738`, size `16`, with `triangleLikeCount=2`, `u16Count=6`, `minIndexLike=487`, `maxIndexLike=490`. | Tiny primitive-group descriptor, likely a very small piece or cap polygon group. | STRONGLY SUPPORTED | Check whether this subrange corresponds to a tiny eye/shadow/attachment piece. |
| Descriptor at `0xBD10 + 600` | Internal block points at `0x10748`, size `192`, with `triangleLikeCount=32`, `u16Count=96`, `minIndexLike=491`, `maxIndexLike=515`. | Strong file-side candidate for a final triangle-index stream covering the highest vertex range in the Crystal vertex-like payload. | STRONGLY SUPPORTED | Prove whether these five primitive groups correspond to distinct sprite-mesh pieces or draw buckets. |

## Attribute / Semantic Path

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| `0x7FF7A7F66130` | Parses per-parameter property strings with keys from a table: `sem`, `tc`, `id`, `inst`, `dt`. Error strings explicitly mention `missing value for tc property`, `missing value for sem property`, and `corrupt properties`. | This is a real semantic/property parser for render parameters, not generic string handling. | CONFIRMED | Dump one real property string and compare it to the runtime parameter objects feeding this path. |
| `0x7FF7A7F66510` | Uses the string `unknown semantic for sem property of param %s: %s`, enforces `property tc is for texture parameters only`, and reports `vertex attribute %s not found for texcoord of parameter %s`. It writes a matched attribute index into `*(WORD *)(a1 + 22)`. | This proves the engine has explicit texture-coordinate attribute linking and semantic-name resolution machinery. | CONFIRMED | Recover the semantic enum table behind `sub_7FF7A7F599D0` so the type codes can be named safely. |
| `0x7FF7A7F650F0` | Builds a sibling-name stem by stripping `_transform`, then matches related attributes and updates remap tables for type codes `44`, `45`, and `46` while checking `texcoord scale/offset value type`. | This is strong evidence for paired attribute metadata such as texcoord transform / scale / offset companions, which fits a textured 2.5D mesh pipeline more than a pure volumetric model. | STRONGLY SUPPORTED | Tie one actual Crystal parameter/material record to this property pipeline. |

## Renderer Boundary

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| current file-side bridge | Mesh-owned records now provably reach float-rich, small-integer-rich, normalized-weight-like, and binding/reference-like regions in both runtimes. | A read-only mesh inspector and future targeted extractor are justified now. | CONFIRMED | Keep extending the offline inspector over these owner-linked regions. |
| current executable-side proof | The engine-side attribute and triangle helpers are real, and the Crystal file-side stage-0 layout now strongly supports “one 28-byte vertex-like stream plus five primitive-group descriptors”, but no direct tie yet proves the exact POSITION lane, UV lane, or the specific unpack path for these exact stage-owned targets. | A first orthographic flat sprite-mesh preview is closer, but still not safe to call fully proven. | NOT YET KNOWN | Recover the exact mesh-owner reader or one stage-0 target consumer before rendering. |
| experimental Crystal candidate preview in `generated/experimental/dragon_crystal_lady_runtime/` | Using the exact `516 x 28` stage-0 candidate records plus the five descriptor-owned primitive payloads, the raw-index preview only covers vertices `0..243`, while a second experimental variant that offsets groups `1..4` by their claimed `minIndexLike` values covers `0..515` exactly and produces a clearly recognizable Crystal Lady silhouette with coherent non-flipped atlas mapping against the confirmed DDS body texture. | This is strong visual support that the candidate record really is an interleaved vertex stream and that the later stage-0 targets are triangle lists whose groups `1..4` store local submesh indices. | STRONGLY SUPPORTED | Tie the same stage-0 targets to the executable mesh reader before promoting POSITION/UV semantics from experimental to confirmed. |

## Static Render Validation

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| Crystal generic render in `generated/experimental/dragon_crystal_lady_runtime/render_runtime_mesh/` | The promoted generic renderer preserves all three coordinates, chooses `XZ` as the best projection, and reproduces a coherent Crystal Lady body silhouette from the same stage-0 geometry using the confirmed `dragon_crystal_lady.tga` DDS payload. | `XZ` is now the best-supported visual plane for Crystal, and the `516 x 28` stream should be promoted to `VISUALLY VALIDATED / FILE-STRUCTURALLY VALIDATED` rather than only a structural hypothesis. | CONFIRMED | Continue executable-side work for lane semantics and binding, not for basic static geometry existence. |
| Crystal per-group renders | `group_0` renders the main torso/tail shell, `group_1` renders head/front-limb and chest pieces, `group_2` renders crest/mane pieces, `group_3` is a tiny isolated island, and `group_4` is a detached body-textured accessory/body cluster. | The primitive-group/base-vertex model is validated enough to talk about distinct sprite-mesh pieces rather than one undifferentiated triangle soup. | STRONGLY SUPPORTED | Map these groups to material or node bindings in the runtime reader. |
| Crystal eye-role render | Rendering the same adjusted geometry with `eye_crystal_lady.tga` produces repeated eye-shaped islands across the mesh, and `group_3` rendered alone becomes one clean isolated eye. | Eye texture binding is real, and `group_3` is a strong eye-pass candidate. Detached body-textured islands that are not eye-only now point more toward transform/binding or additional material-pass work than toward a failed main vertex/index decode. | STRONGLY SUPPORTED | Recover the exact group-to-eye binding in the binding table or executable-side material reader. |
| Crystal missing-role renders | `fx_spark_alpha.tga` and `shadow_dragon.tga` still do not resolve from the currently accessible local files and scanned DLC BUD set, so they are rendered only in debug color. | Effect and shadow passes remain dependency/binding problems, not main-mesh decode problems. | CONFIRMED | Find another archive/source for those textures or tie them to a non-textured pass. |
| Metal generic render in `generated/experimental/dragon_metal_seahorse_runtime/render_runtime_mesh/` | The same renderer auto-detects a different stage-0 stream: `529` records, `24` bytes each, layout guess `float3 + float2 + marker`, four primitive groups, claim-offset reconstruction covering `0..528`, and `XZ` again as the best projection. The body render produces a coherent Metal Seahorse silhouette. | The static extraction path now generalizes beyond Crystal. The runtime family has at least two experimentally validated stage-0 geometry layouts: Crystal `28`-byte and Metal `24`-byte. | STRONGLY SUPPORTED | Prove the meaning of the Metal marker word and tie both layouts to the executable-side reader. |

## Two-Dragon Static Render Validation

| Address or file offset | Raw observation | Interpretation | Confidence | How to test/confirm next |
| --- | --- | --- | --- | --- |
| Crystal validation checks | The promoted generic renderer now records exact validation checks for Crystal: payload size equals `516 * 28`, all adjusted indices stay in bounds, every claimed primitive range matches the adjusted observed range, and the adjusted primitive groups cover all vertices `0..515`. | Crystal stage-0 geometry has crossed from candidate-only status into a reusable static viewer/exporter baseline. | CONFIRMED | Keep the generated Crystal PNG/OBJ/glTF set as a regression fixture. |
| Metal validation checks | The same checks pass for Metal: payload size equals `529 * 24`, all adjusted indices stay in bounds, every claimed primitive range matches the adjusted observed range, and the adjusted primitive groups cover all vertices `0..528`. | Metal independently confirms the mesh-owner/stage/primitive reconstruction model. | CONFIRMED | Add more runtime samples later, but the main static body-mesh path is already validated across two dragons. |
| alternate Tiki validation checks | The refreshed `dragon_tiki_20260613_144153` session now passes with `372 * 24` stage-0 vertices, layout `float3 + marker + float2`, two primitive groups, and full adjusted coverage `0..371`. Its primitive payload targets sit before `dataStart`, which the earlier scanner incorrectly rejected. | The static mesh path handles another real runtime variant: pre-`dataStart` stage-owned index payloads plus marker-at-`+0x0C` 24-byte vertices. | CONFIRMED | Preserve this session as a regression sample for non-Crystal/non-Metal geometry layout variants. |
| Crystal role hints | Cross-role centroid-alpha checks now keep `group_0`, `group_1`, and `group_2` as strong body candidates; isolate `group_3` as a strong eye candidate; and leave `group_4` as a detached body-like/unresolved cluster with UVs extending outside `0..1`. | Eye binding is partially separable already, while at least one detached island still looks more like a binding/pass problem than a geometry decode failure. | STRONGLY SUPPORTED | Tie group-to-pass assignment to record-backed bindings instead of visual evidence alone. |
| Metal role hints | Metal keeps `group_0` and `group_2` as strong body candidates, leaves tiny `group_1` unresolved from visual evidence alone, and marks `group_3` as an unresolved alternate pass because both body and eye centroid alpha remain zero and its UVs are off-atlas. | Metal likely has at least one non-body/non-eye subpass still requiring explicit binding recovery. | STRONGLY SUPPORTED | Recover shadow/effect/alternate-pass binding records and compare them to the `group_3` descriptor. |
